/* automatically generated by rust-bindgen 0.70.1 */

pub const GRK_NUM_COMMENTS_SUPPORTED: u32 = 256;
pub const GRK_NUM_ASOC_BOXES_SUPPORTED: u32 = 256;
pub const GRK_NUM_XML_BOXES_SUPPORTED: u32 = 64;
pub const GRK_MAX_SUPPORTED_IMAGE_PRECISION: u32 = 16;
pub const GRK_BIBO_EXTRA_BITS: u32 = 7;
pub const GRK_MAX_PASSES: u32 = 67;
pub const GRK_PATH_LEN: u32 = 1024;
pub const GRK_MAX_LAYERS: u32 = 256;
pub const GRK_MAX_DECOMP_LVLS: u32 = 32;
pub const GRK_MAXRLVLS: u32 = 33;
pub const GRK_MAXBANDS: u32 = 97;
pub const GRK_MAX_CUSTOM_HEADERS: u32 = 16;
pub const GRK_TILE_CACHE_NONE: u32 = 0;
pub const GRK_TILE_CACHE_IMAGE: u32 = 1;
pub const GRK_TILE_CACHE_ALL: u32 = 2;
pub const GRK_TILE_CACHE_LRU: u32 = 4;
pub const GRK_RANDOM_ACCESS_PLT: u32 = 1;
pub const GRK_RANDOM_ACCESS_TLM: u32 = 2;
pub const GRK_RANDOM_ACCESS_PLM: u32 = 4;
pub const GRK_DECOMPRESS_COMPRESSION_LEVEL_DEFAULT: u32 = 4294967295;
pub const GRK_DECODE_HEADER: u32 = 1;
pub const GRK_DECODE_T2: u32 = 2;
pub const GRK_DECODE_T1: u32 = 4;
pub const GRK_DECODE_POST_T1: u32 = 8;
pub const GRK_PLUGIN_DECODE_CLEAN: u32 = 16;
pub const GRK_DECODE_ALL: u32 = 31;
pub const GRK_IMG_INFO: u32 = 1;
pub const GRK_MH_INFO: u32 = 2;
pub const GRK_TH_INFO: u32 = 4;
pub const GRK_TCH_INFO: u32 = 8;
pub const GRK_MH_IND: u32 = 16;
pub const GRK_TH_IND: u32 = 32;
pub const GRK_CBLKSTY_LAZY: u32 = 1;
pub const GRK_CBLKSTY_RESET: u32 = 2;
pub const GRK_CBLKSTY_TERMALL: u32 = 4;
pub const GRK_CBLKSTY_VSC: u32 = 8;
pub const GRK_CBLKSTY_PTERM: u32 = 16;
pub const GRK_CBLKSTY_SEGSYM: u32 = 32;
pub const GRK_CBLKSTY_HT_ONLY: u32 = 64;
pub const GRK_CBLKSTY_HT_MIXED: u32 = 192;
pub const GRK_JPH_RSIZ_FLAG: u32 = 16384;
pub const GRK_PROFILE_NONE: u32 = 0;
pub const GRK_PROFILE_0: u32 = 1;
pub const GRK_PROFILE_1: u32 = 2;
pub const GRK_PROFILE_CINEMA_2K: u32 = 3;
pub const GRK_PROFILE_CINEMA_4K: u32 = 4;
pub const GRK_PROFILE_CINEMA_S2K: u32 = 5;
pub const GRK_PROFILE_CINEMA_S4K: u32 = 6;
pub const GRK_PROFILE_CINEMA_LTS: u32 = 7;
pub const GRK_PROFILE_BC_SINGLE: u32 = 256;
pub const GRK_PROFILE_BC_MULTI: u32 = 512;
pub const GRK_PROFILE_BC_MULTI_R: u32 = 768;
pub const GRK_PROFILE_BC_MASK: u32 = 783;
pub const GRK_PROFILE_IMF_2K: u32 = 1024;
pub const GRK_PROFILE_IMF_4K: u32 = 1280;
pub const GRK_PROFILE_IMF_8K: u32 = 1536;
pub const GRK_PROFILE_IMF_2K_R: u32 = 1792;
pub const GRK_PROFILE_IMF_4K_R: u32 = 2048;
pub const GRK_PROFILE_IMF_8K_R: u32 = 2304;
pub const GRK_PROFILE_MASK: u32 = 4095;
pub const GRK_PROFILE_PART2: u32 = 32768;
pub const GRK_PROFILE_PART2_EXTENSIONS_MASK: u32 = 16383;
pub const GRK_EXTENSION_NONE: u32 = 0;
pub const GRK_EXTENSION_MCT: u32 = 256;
pub const GRK_LEVEL_MAX: u32 = 11;
pub const GRK_BROADCAST_LEVEL_1_MBITSSEC: u32 = 200;
pub const GRK_BROADCAST_LEVEL_2_MBITSSEC: u32 = 200;
pub const GRK_BROADCAST_LEVEL_3_MBITSSEC: u32 = 200;
pub const GRK_BROADCAST_LEVEL_4_MBITSSEC: u32 = 400;
pub const GRK_BROADCAST_LEVEL_5_MBITSSEC: u32 = 800;
pub const GRK_BROADCAST_LEVEL_6_MBITSSEC: u32 = 1600;
pub const GRK_BROADCAST_LEVEL_7_MBITSSEC: u32 = 3200;
pub const GRK_BROADCAST_LEVEL_8_MBITSSEC: u32 = 6400;
pub const GRK_BROADCAST_LEVEL_9_MBITSSEC: u32 = 12800;
pub const GRK_BROADCAST_LEVEL_10_MBITSSEC: u32 = 25600;
pub const GRK_BROADCAST_LEVEL_11_MBITSSEC: u32 = 51200;
pub const GRK_BROADCAST_LEVEL_1_MSAMPLESSEC: u32 = 64;
pub const GRK_BROADCAST_LEVEL_2_MSAMPLESSEC: u32 = 130;
pub const GRK_BROADCAST_LEVEL_3_MSAMPLESSEC: u32 = 195;
pub const GRK_BROADCAST_LEVEL_4_MSAMPLESSEC: u32 = 260;
pub const GRK_BROADCAST_LEVEL_5_MSAMPLESSEC: u32 = 520;
pub const GRK_BROADCAST_LEVEL_6_MSAMPLESSEC: u32 = 1200;
pub const GRK_BROADCAST_LEVEL_7_MSAMPLESSEC: u32 = 2400;
pub const GRK_BROADCAST_LEVEL_8_MSAMPLESSEC: u32 = 4800;
pub const GRK_BROADCAST_LEVEL_9_MSAMPLESSEC: u32 = 9600;
pub const GRK_BROADCAST_LEVEL_10_MSAMPLESSEC: u32 = 19200;
pub const GRK_BROADCAST_LEVEL_11_MSAMPLESSEC: u32 = 38400;
pub const GRK_IMF_MAINLEVEL_1_MSAMPLESSEC: u32 = 65;
pub const GRK_IMF_MAINLEVEL_2_MSAMPLESSEC: u32 = 130;
pub const GRK_IMF_MAINLEVEL_3_MSAMPLESSEC: u32 = 195;
pub const GRK_IMF_MAINLEVEL_4_MSAMPLESSEC: u32 = 260;
pub const GRK_IMF_MAINLEVEL_5_MSAMPLESSEC: u32 = 520;
pub const GRK_IMF_MAINLEVEL_6_MSAMPLESSEC: u32 = 1200;
pub const GRK_IMF_MAINLEVEL_7_MSAMPLESSEC: u32 = 2400;
pub const GRK_IMF_MAINLEVEL_8_MSAMPLESSEC: u32 = 4800;
pub const GRK_IMF_MAINLEVEL_9_MSAMPLESSEC: u32 = 9600;
pub const GRK_IMF_MAINLEVEL_10_MSAMPLESSEC: u32 = 19200;
pub const GRK_IMF_MAINLEVEL_11_MSAMPLESSEC: u32 = 38400;
pub const GRK_IMF_SUBLEVEL_MAX: u32 = 9;
pub const GRK_IMF_SUBLEVEL_1_MBITSSEC: u32 = 200;
pub const GRK_IMF_SUBLEVEL_2_MBITSSEC: u32 = 400;
pub const GRK_IMF_SUBLEVEL_3_MBITSSEC: u32 = 800;
pub const GRK_IMF_SUBLEVEL_4_MBITSSEC: u32 = 1600;
pub const GRK_IMF_SUBLEVEL_5_MBITSSEC: u32 = 3200;
pub const GRK_IMF_SUBLEVEL_6_MBITSSEC: u32 = 6400;
pub const GRK_IMF_SUBLEVEL_7_MBITSSEC: u32 = 12800;
pub const GRK_IMF_SUBLEVEL_8_MBITSSEC: u32 = 25600;
pub const GRK_IMF_SUBLEVEL_9_MBITSSEC: u32 = 51200;
pub const GRK_CINEMA_DCI_MAX_BANDWIDTH: u32 = 250000000;
pub const GRK_CINEMA_24_CS: u32 = 1302083;
pub const GRK_CINEMA_24_COMP: u32 = 1041666;
pub const GRK_CINEMA_48_CS: u32 = 651041;
pub const GRK_CINEMA_48_COMP: u32 = 520833;
pub const GRK_CINEMA_4K_DEFAULT_NUM_RESOLUTIONS: u32 = 7;
pub const GRK_CUSTOM_CIELAB_SPACE: u32 = 0;
pub const GRK_DEFAULT_CIELAB_SPACE: u32 = 1145390592;
pub const GRK_PLUGIN_STATE_NO_DEBUG: u32 = 0;
pub const GRK_PLUGIN_STATE_DEBUG: u32 = 1;
pub const GRK_PLUGIN_STATE_PRE_TR1: u32 = 2;
pub const GRK_PLUGIN_STATE_DWT_QUANTIZATION: u32 = 4;
pub const GRK_PLUGIN_STATE_MCT_ONLY: u32 = 8;
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct _iobuf {
    pub _Placeholder: *mut ::std::os::raw::c_void,
}
pub type FILE = _iobuf;
pub const _GRK_PROG_ORDER_GRK_PROG_UNKNOWN: _GRK_PROG_ORDER = -1;
#[doc = " unknown progression order"]
pub const _GRK_PROG_ORDER_GRK_LRCP: _GRK_PROG_ORDER = 0;
#[doc = " layer-resolution-component-precinct order"]
pub const _GRK_PROG_ORDER_GRK_RLCP: _GRK_PROG_ORDER = 1;
#[doc = " resolution-layer-component-precinct order"]
pub const _GRK_PROG_ORDER_GRK_RPCL: _GRK_PROG_ORDER = 2;
#[doc = " resolution-precinct-component-layer order"]
pub const _GRK_PROG_ORDER_GRK_PCRL: _GRK_PROG_ORDER = 3;
#[doc = " precinct-component-resolution-layer order"]
pub const _GRK_PROG_ORDER_GRK_CPRL: _GRK_PROG_ORDER = 4;
#[doc = " component-precinct-resolution-layer order"]
pub const _GRK_PROG_ORDER_GRK_NUM_PROGRESSION_ORDERS: _GRK_PROG_ORDER = 5;
#[doc = " @brief Progression orders"]
pub type _GRK_PROG_ORDER = ::std::os::raw::c_int;
#[doc = " @brief Progression orders"]
pub use self::_GRK_PROG_ORDER as GRK_PROG_ORDER;
pub const _GRK_COLOR_SPACE_GRK_CLRSPC_UNKNOWN: _GRK_COLOR_SPACE = 0;
#[doc = " unknown"]
pub const _GRK_COLOR_SPACE_GRK_CLRSPC_SRGB: _GRK_COLOR_SPACE = 2;
#[doc = " sRGB"]
pub const _GRK_COLOR_SPACE_GRK_CLRSPC_GRAY: _GRK_COLOR_SPACE = 3;
#[doc = " grayscale"]
pub const _GRK_COLOR_SPACE_GRK_CLRSPC_SYCC: _GRK_COLOR_SPACE = 4;
#[doc = " standard YCC (YUV)"]
pub const _GRK_COLOR_SPACE_GRK_CLRSPC_EYCC: _GRK_COLOR_SPACE = 5;
#[doc = " extended YCC"]
pub const _GRK_COLOR_SPACE_GRK_CLRSPC_CMYK: _GRK_COLOR_SPACE = 6;
#[doc = " CMYK"]
pub const _GRK_COLOR_SPACE_GRK_CLRSPC_DEFAULT_CIE: _GRK_COLOR_SPACE = 7;
#[doc = " default CIE LAB"]
pub const _GRK_COLOR_SPACE_GRK_CLRSPC_CUSTOM_CIE: _GRK_COLOR_SPACE = 8;
#[doc = " custom CIE LAB"]
pub const _GRK_COLOR_SPACE_GRK_CLRSPC_ICC: _GRK_COLOR_SPACE = 9;
#[doc = " @brief Grok supported color spaces"]
pub type _GRK_COLOR_SPACE = ::std::os::raw::c_int;
#[doc = " @brief Grok supported color spaces"]
pub use self::_GRK_COLOR_SPACE as GRK_COLOR_SPACE;
pub const _GRK_ENUM_COLOUR_SPACE_GRK_ENUM_CLRSPC_UNKNOWN: _GRK_ENUM_COLOUR_SPACE = -1;
pub const _GRK_ENUM_COLOUR_SPACE_GRK_ENUM_CLRSPC_BILEVEL1: _GRK_ENUM_COLOUR_SPACE = 0;
pub const _GRK_ENUM_COLOUR_SPACE_GRK_ENUM_CLRSPC_YCBCR1: _GRK_ENUM_COLOUR_SPACE = 1;
pub const _GRK_ENUM_COLOUR_SPACE_GRK_ENUM_CLRSPC_YCBCR2: _GRK_ENUM_COLOUR_SPACE = 3;
pub const _GRK_ENUM_COLOUR_SPACE_GRK_ENUM_CLRSPC_YCBCR3: _GRK_ENUM_COLOUR_SPACE = 4;
pub const _GRK_ENUM_COLOUR_SPACE_GRK_ENUM_CLRSPC_PHOTO_YCC: _GRK_ENUM_COLOUR_SPACE = 9;
pub const _GRK_ENUM_COLOUR_SPACE_GRK_ENUM_CLRSPC_CMY: _GRK_ENUM_COLOUR_SPACE = 11;
pub const _GRK_ENUM_COLOUR_SPACE_GRK_ENUM_CLRSPC_CMYK: _GRK_ENUM_COLOUR_SPACE = 12;
pub const _GRK_ENUM_COLOUR_SPACE_GRK_ENUM_CLRSPC_YCCK: _GRK_ENUM_COLOUR_SPACE = 13;
pub const _GRK_ENUM_COLOUR_SPACE_GRK_ENUM_CLRSPC_CIE: _GRK_ENUM_COLOUR_SPACE = 14;
pub const _GRK_ENUM_COLOUR_SPACE_GRK_ENUM_CLRSPC_BILEVEL2: _GRK_ENUM_COLOUR_SPACE = 15;
pub const _GRK_ENUM_COLOUR_SPACE_GRK_ENUM_CLRSPC_SRGB: _GRK_ENUM_COLOUR_SPACE = 16;
pub const _GRK_ENUM_COLOUR_SPACE_GRK_ENUM_CLRSPC_GRAY: _GRK_ENUM_COLOUR_SPACE = 17;
pub const _GRK_ENUM_COLOUR_SPACE_GRK_ENUM_CLRSPC_SYCC: _GRK_ENUM_COLOUR_SPACE = 18;
pub const _GRK_ENUM_COLOUR_SPACE_GRK_ENUM_CLRSPC_CIEJAB: _GRK_ENUM_COLOUR_SPACE = 19;
pub const _GRK_ENUM_COLOUR_SPACE_GRK_ENUM_CLRSPC_ESRGB: _GRK_ENUM_COLOUR_SPACE = 20;
pub const _GRK_ENUM_COLOUR_SPACE_GRK_ENUM_CLRSPC_ROMMRGB: _GRK_ENUM_COLOUR_SPACE = 21;
pub const _GRK_ENUM_COLOUR_SPACE_GRK_ENUM_CLRSPC_YPBPR60: _GRK_ENUM_COLOUR_SPACE = 22;
pub const _GRK_ENUM_COLOUR_SPACE_GRK_ENUM_CLRSPC_YPBPR50: _GRK_ENUM_COLOUR_SPACE = 23;
pub const _GRK_ENUM_COLOUR_SPACE_GRK_ENUM_CLRSPC_EYCC: _GRK_ENUM_COLOUR_SPACE = 24;
#[doc = " @brief JPEG 2000 enumerated color spaces"]
pub type _GRK_ENUM_COLOUR_SPACE = ::std::os::raw::c_int;
#[doc = " @brief JPEG 2000 enumerated color spaces"]
pub use self::_GRK_ENUM_COLOUR_SPACE as GRK_ENUM_COLOUR_SPACE;
#[doc = " @brief Logging callback\n @param msg message\n @param client_data client data passed to callback"]
pub type grk_msg_callback = ::std::option::Option<
    unsafe extern "C" fn(
        msg: *const ::std::os::raw::c_char,
        client_data: *mut ::std::os::raw::c_void,
    ),
>;
#[doc = " @struct grk_msg_handlers\n @brief Logging handlers\n @param info_callback info callback (see @ref grk_msg_callback)\n @param info_data info data\n @param warn_callback warn callback (see @ref grk_msg_callback)\n @param warn_data warn data\n @param error_callback error callback (see @ref grk_msg_callback)\n @param error_data error data"]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct _grk_msg_handlers {
    pub info_callback: grk_msg_callback,
    pub info_data: *mut ::std::os::raw::c_void,
    pub debug_callback: grk_msg_callback,
    pub debug_data: *mut ::std::os::raw::c_void,
    pub trace_callback: grk_msg_callback,
    pub trace_data: *mut ::std::os::raw::c_void,
    pub warn_callback: grk_msg_callback,
    pub warn_data: *mut ::std::os::raw::c_void,
    pub error_callback: grk_msg_callback,
    pub error_data: *mut ::std::os::raw::c_void,
}
#[doc = " @struct grk_msg_handlers\n @brief Logging handlers\n @param info_callback info callback (see @ref grk_msg_callback)\n @param info_data info data\n @param warn_callback warn callback (see @ref grk_msg_callback)\n @param warn_data warn data\n @param error_callback error callback (see @ref grk_msg_callback)\n @param error_data error data"]
pub type grk_msg_handlers = _grk_msg_handlers;
#[doc = " @struct grk_object\n @brief Opaque reference-counted object"]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct _grk_object {
    pub wrapper: *mut ::std::os::raw::c_void,
}
#[doc = " @struct grk_object\n @brief Opaque reference-counted object"]
pub type grk_object = _grk_object;
#[doc = " @struct grk_progression\n @brief Progression order change (POC)"]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct _grk_progression {
    pub progression: GRK_PROG_ORDER,
    pub progression_str: [::std::os::raw::c_char; 5usize],
    pub specified_compression_poc_prog: GRK_PROG_ORDER,
    pub tileno: u32,
    #[doc = " tile dimensions"]
    pub tx0: u32,
    pub ty0: u32,
    pub tx1: u32,
    pub ty1: u32,
    #[doc = " progression order bounds specified by POC"]
    pub comp_s: u16,
    pub comp_e: u16,
    pub res_s: u8,
    pub res_e: u8,
    pub prec_s: u64,
    pub prec_e: u64,
    pub lay_s: u16,
    pub lay_e: u16,
    pub tp_comp_s: u16,
    pub tp_comp_e: u16,
    pub tp_res_s: u8,
    pub tp_res_e: u8,
    pub tp_prec_e: u64,
    pub tp_lay_e: u16,
    pub tp_tx_s: u32,
    pub tp_tx_e: u32,
    pub tp_ty_s: u32,
    pub tp_ty_e: u32,
    pub dx: u32,
    pub dy: u32,
    pub comp_temp: u16,
    pub res_temp: u8,
    pub prec_temp: u64,
    pub lay_temp: u16,
    pub tx0_temp: u32,
    pub ty0_temp: u32,
}
#[doc = " @struct grk_progression\n @brief Progression order change (POC)"]
pub type grk_progression = _grk_progression;
#[doc = " @struct grk_raw_comp_cparameters\n @brief RAW component compress parameters"]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct _grk_raw_comp_cparameters {
    pub dx: u8,
    #[doc = " subsampling in X direction"]
    pub dy: u8,
}
#[doc = " @struct grk_raw_comp_cparameters\n @brief RAW component compress parameters"]
pub type grk_raw_comp_cparameters = _grk_raw_comp_cparameters;
#[doc = " @struct grk_raw_cparameters\n @brief RAW image compress parameters"]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct _grk_raw_cparameters {
    pub width: u32,
    #[doc = " width"]
    pub height: u32,
    #[doc = " height"]
    pub numcomps: u16,
    #[doc = " number of components"]
    pub prec: u8,
    #[doc = " bit depth"]
    pub sgnd: bool,
    #[doc = " signed/unsigned"]
    pub comps: *mut grk_raw_comp_cparameters,
}
#[doc = " @struct grk_raw_cparameters\n @brief RAW image compress parameters"]
pub type grk_raw_cparameters = _grk_raw_cparameters;
pub const _GRK_RATE_CONTROL_ALGORITHM_GRK_RATE_CONTROL_BISECT: _GRK_RATE_CONTROL_ALGORITHM = 0;
pub const _GRK_RATE_CONTROL_ALGORITHM_GRK_RATE_CONTROL_PCRD_OPT: _GRK_RATE_CONTROL_ALGORITHM = 1;
#[doc = " @brief Rate control algorithms\n @param GRK_RATE_CONTROL_BISECT: bisect with all truncation points\n @param GRK_RATE_CONTROL_PCRD_OPT: PCRD: bisect with only feasible truncation points"]
pub type _GRK_RATE_CONTROL_ALGORITHM = ::std::os::raw::c_int;
#[doc = " @brief Rate control algorithms\n @param GRK_RATE_CONTROL_BISECT: bisect with all truncation points\n @param GRK_RATE_CONTROL_PCRD_OPT: PCRD: bisect with only feasible truncation points"]
pub use self::_GRK_RATE_CONTROL_ALGORITHM as GRK_RATE_CONTROL_ALGORITHM;
pub const _GRK_SUPPORTED_FILE_FMT_GRK_FMT_UNK: _GRK_SUPPORTED_FILE_FMT = 0;
pub const _GRK_SUPPORTED_FILE_FMT_GRK_FMT_J2K: _GRK_SUPPORTED_FILE_FMT = 1;
pub const _GRK_SUPPORTED_FILE_FMT_GRK_FMT_JP2: _GRK_SUPPORTED_FILE_FMT = 2;
pub const _GRK_SUPPORTED_FILE_FMT_GRK_FMT_PXM: _GRK_SUPPORTED_FILE_FMT = 3;
pub const _GRK_SUPPORTED_FILE_FMT_GRK_FMT_PGX: _GRK_SUPPORTED_FILE_FMT = 4;
pub const _GRK_SUPPORTED_FILE_FMT_GRK_FMT_PAM: _GRK_SUPPORTED_FILE_FMT = 5;
pub const _GRK_SUPPORTED_FILE_FMT_GRK_FMT_BMP: _GRK_SUPPORTED_FILE_FMT = 6;
pub const _GRK_SUPPORTED_FILE_FMT_GRK_FMT_TIF: _GRK_SUPPORTED_FILE_FMT = 7;
pub const _GRK_SUPPORTED_FILE_FMT_GRK_FMT_RAW: _GRK_SUPPORTED_FILE_FMT = 8;
pub const _GRK_SUPPORTED_FILE_FMT_GRK_FMT_PNG: _GRK_SUPPORTED_FILE_FMT = 9;
pub const _GRK_SUPPORTED_FILE_FMT_GRK_FMT_RAWL: _GRK_SUPPORTED_FILE_FMT = 10;
pub const _GRK_SUPPORTED_FILE_FMT_GRK_FMT_JPG: _GRK_SUPPORTED_FILE_FMT = 11;
pub const _GRK_SUPPORTED_FILE_FMT_GRK_FMT_MJ2: _GRK_SUPPORTED_FILE_FMT = 12;
#[doc = " @brief All Grok supported file formats"]
pub type _GRK_SUPPORTED_FILE_FMT = ::std::os::raw::c_int;
#[doc = " @brief All Grok supported file formats"]
pub use self::_GRK_SUPPORTED_FILE_FMT as GRK_SUPPORTED_FILE_FMT;
pub const _GRK_CODEC_FORMAT_GRK_CODEC_UNK: _GRK_CODEC_FORMAT = 0;
#[doc = " unknown format"]
pub const _GRK_CODEC_FORMAT_GRK_CODEC_J2K: _GRK_CODEC_FORMAT = 1;
#[doc = " JPEG 2000 code-stream"]
pub const _GRK_CODEC_FORMAT_GRK_CODEC_JP2: _GRK_CODEC_FORMAT = 2;
#[doc = " JPEG 2000 JP2 file format"]
pub const _GRK_CODEC_FORMAT_GRK_CODEC_MJ2: _GRK_CODEC_FORMAT = 3;
#[doc = " @brief Grok Supported JPEG 2000 formats"]
pub type _GRK_CODEC_FORMAT = ::std::os::raw::c_int;
#[doc = " @brief Grok Supported JPEG 2000 formats"]
pub use self::_GRK_CODEC_FORMAT as GRK_CODEC_FORMAT;
#[doc = " @struct grk_component_mapping_comp\n @brief Component mappings: component index, mapping type, palette column\n Note: \"component\" refers to an image component as decompressed\n from the code stream, while \"channel\" refers to a component resulting\n from the application of a Palette box LUT and a Component mapping box."]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct _grk_component_mapping_comp {
    pub component: u16,
    pub mapping_type: u8,
    pub palette_column: u8,
}
#[doc = " @struct grk_component_mapping_comp\n @brief Component mappings: component index, mapping type, palette column\n Note: \"component\" refers to an image component as decompressed\n from the code stream, while \"channel\" refers to a component resulting\n from the application of a Palette box LUT and a Component mapping box."]
pub type grk_component_mapping_comp = _grk_component_mapping_comp;
#[doc = " @struct grk_palette_data\n @brief Palette data"]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct _grk_palette_data {
    pub lut: *mut i32,
    pub num_entries: u16,
    pub component_mapping: *mut grk_component_mapping_comp,
    pub num_channels: u8,
    pub channel_sign: *mut bool,
    pub channel_prec: *mut u8,
}
#[doc = " @struct grk_palette_data\n @brief Palette data"]
pub type grk_palette_data = _grk_palette_data;
pub const _GRK_CHANNEL_TYPE_GRK_CHANNEL_TYPE_COLOUR: _GRK_CHANNEL_TYPE = 0;
pub const _GRK_CHANNEL_TYPE_GRK_CHANNEL_TYPE_OPACITY: _GRK_CHANNEL_TYPE = 1;
pub const _GRK_CHANNEL_TYPE_GRK_CHANNEL_TYPE_PREMULTIPLIED_OPACITY: _GRK_CHANNEL_TYPE = 2;
pub const _GRK_CHANNEL_TYPE_GRK_CHANNEL_TYPE_UNSPECIFIED: _GRK_CHANNEL_TYPE = 65535;
pub type _GRK_CHANNEL_TYPE = ::std::os::raw::c_int;
pub use self::_GRK_CHANNEL_TYPE as GRK_CHANNEL_TYPE;
pub const _GRK_CHANNEL_ASSOC_GRK_CHANNEL_ASSOC_WHOLE_IMAGE: _GRK_CHANNEL_ASSOC = 0;
pub const _GRK_CHANNEL_ASSOC_GRK_CHANNEL_ASSOC_COLOUR_1: _GRK_CHANNEL_ASSOC = 1;
pub const _GRK_CHANNEL_ASSOC_GRK_CHANNEL_ASSOC_COLOUR_2: _GRK_CHANNEL_ASSOC = 2;
pub const _GRK_CHANNEL_ASSOC_GRK_CHANNEL_ASSOC_COLOUR_3: _GRK_CHANNEL_ASSOC = 3;
pub const _GRK_CHANNEL_ASSOC_GRK_CHANNEL_ASSOC_UNASSOCIATED: _GRK_CHANNEL_ASSOC = 65535;
#[doc = " @brief Channel association"]
pub type _GRK_CHANNEL_ASSOC = ::std::os::raw::c_int;
#[doc = " @brief Channel association"]
pub use self::_GRK_CHANNEL_ASSOC as GRK_CHANNEL_ASSOC;
#[doc = " @struct grk_channel_description\n @brief Channel definition: channel index, type, association"]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct _grk_channel_description {
    pub channel: u16,
    pub typ: u16,
    pub asoc: u16,
}
#[doc = " @struct grk_channel_description\n @brief Channel definition: channel index, type, association"]
pub type grk_channel_description = _grk_channel_description;
#[doc = " @struct grk_channel_definition\n @brief Channel definition"]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct _grk_channel_definition {
    pub descriptions: *mut grk_channel_description,
    pub num_channel_descriptions: u16,
}
#[doc = " @struct grk_channel_definition\n @brief Channel definition"]
pub type grk_channel_definition = _grk_channel_definition;
#[doc = " @struct grk_asoc\n @brief Association box info"]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct _grk_asoc {
    pub level: u32,
    pub label: *const ::std::os::raw::c_char,
    pub xml: *mut u8,
    pub xml_len: u32,
}
#[doc = " @struct grk_asoc\n @brief Association box info"]
pub type grk_asoc = _grk_asoc;
pub const _grk_precision_mode_GRK_PREC_MODE_CLIP: _grk_precision_mode = 0;
pub const _grk_precision_mode_GRK_PREC_MODE_SCALE: _grk_precision_mode = 1;
#[doc = " @brief Precision mode"]
pub type _grk_precision_mode = ::std::os::raw::c_int;
#[doc = " @brief Precision mode"]
pub use self::_grk_precision_mode as grk_precision_mode;
#[doc = " @struct grk_precision\n @brief Precision"]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct _grk_precision {
    pub prec: u8,
    pub mode: grk_precision_mode,
}
#[doc = " @struct grk_precision\n @brief Precision"]
pub type grk_precision = _grk_precision;
#[doc = " @struct grk_rescale\n @brief Linear value-range rescale:\n        out = round((in - src_min) * (dst_max - dst_min) / (src_max - src_min)) + dst_min,\n        clamped to [min(dst_min,dst_max), max(dst_min,dst_max)].\n        Applied per-component before any precision adjustment. Component prec/sgnd\n        are updated to fit the dst range."]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct _grk_rescale {
    pub src_min: f64,
    pub src_max: f64,
    pub dst_min: f64,
    pub dst_max: f64,
}
#[doc = " @struct grk_rescale\n @brief Linear value-range rescale:\n        out = round((in - src_min) * (dst_max - dst_min) / (src_max - src_min)) + dst_min,\n        clamped to [min(dst_min,dst_max), max(dst_min,dst_max)].\n        Applied per-component before any precision adjustment. Component prec/sgnd\n        are updated to fit the dst range."]
pub type grk_rescale = _grk_rescale;
#[doc = " @struct grk_progression_state\n @brief Stores progression state information\n Note: limited to 256 components"]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct _grk_progression_state {
    pub num_resolutions: u8,
    pub layers_per_resolution: [u16; 33usize],
    pub numcomps: u16,
    pub comp: [u16; 256usize],
    pub single_tile: bool,
    pub tile_index: u16,
}
#[doc = " @struct grk_progression_state\n @brief Stores progression state information\n Note: limited to 256 components"]
pub type grk_progression_state = _grk_progression_state;
#[doc = " @struct grk_io_buf\n @brief Grok IO buffer"]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct _grk_io_buf {
    pub data: *mut u8,
    pub offset: usize,
    pub len: usize,
    pub alloc_len: usize,
    pub pooled: bool,
    pub index: u32,
}
#[doc = " @struct grk_io_buf\n @brief Grok IO buffer"]
pub type grk_io_buf = _grk_io_buf;
#[doc = " @struct grk_io_init\n @brief Grok IO initialization"]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct _grk_io_init {
    pub max_pooled_requests: u32,
}
#[doc = " @struct grk_io_init\n @brief Grok IO initialization"]
pub type grk_io_init = _grk_io_init;
#[doc = " @brief Grok IO callback\n @param worker_id worker id\n @param buffer io buffer (see @ref grk_io_buf)\n @param io_user_data user data"]
pub type grk_io_callback = ::std::option::Option<
    unsafe extern "C" fn(
        worker_id: u32,
        buffer: grk_io_buf,
        io_user_data: *mut ::std::os::raw::c_void,
    ) -> bool,
>;
#[doc = " @brief Grok IO register reclaim callback\n @param io_init io initialization (see @ref grk_io_init)\n @param reclaim_callback (see @ref grk_io_callback)\n @param io_user_data io user data\n @param reclaim_user_data reclaim user data"]
pub type grk_io_register_reclaim_callback = ::std::option::Option<
    unsafe extern "C" fn(
        io_init: grk_io_init,
        reclaim_callback: grk_io_callback,
        io_user_data: *mut ::std::os::raw::c_void,
        reclaim_user_data: *mut ::std::os::raw::c_void,
    ),
>;
#[doc = " @brief Grok IO pixels callback\n @param worker_id worker id\n @param buffer Grok io buffer (see @ref grk_io_buf)\n @param user_data user data"]
pub type grk_io_pixels_callback = ::std::option::Option<
    unsafe extern "C" fn(
        worker_id: u32,
        buffer: grk_io_buf,
        user_data: *mut ::std::os::raw::c_void,
    ) -> bool,
>;
#[doc = " @brief Read stream callback\n @param buffer buffer to write stream to\n @param numBytes number of bytes to write to buffer\n @param user_data user data\n"]
pub type grk_stream_read_fn = ::std::option::Option<
    unsafe extern "C" fn(
        buffer: *mut u8,
        numBytes: usize,
        user_data: *mut ::std::os::raw::c_void,
    ) -> usize,
>;
#[doc = " @brief Write stream callback\n @param buffer buffer to read stream from\n @param numBytes number of bytes to read from buffer\n @param user_data user data\n"]
pub type grk_stream_write_fn = ::std::option::Option<
    unsafe extern "C" fn(
        buffer: *const u8,
        numBytes: usize,
        user_data: *mut ::std::os::raw::c_void,
    ) -> usize,
>;
#[doc = " @brief Seek (absolute) callback\n @param offset \t\t\tabsolute stream offset\n @param user_data user \tdata"]
pub type grk_stream_seek_fn = ::std::option::Option<
    unsafe extern "C" fn(offset: u64, user_data: *mut ::std::os::raw::c_void) -> bool,
>;
#[doc = " @brief Free user data callback\n @param user_data user data"]
pub type grk_stream_free_user_data_fn =
    ::std::option::Option<unsafe extern "C" fn(user_data: *mut ::std::os::raw::c_void)>;
#[doc = " @struct grk_stream_params\n @brief JPEG 2000 stream parameters\n There are three methods of streaming: by file, buffer or callback\n Client must choose only one method, and populate this struct accordingly"]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct _grk_stream_params {
    pub initial_offset: usize,
    pub double_buffer_len: usize,
    pub initial_double_buffer_len: usize,
    pub from_network: bool,
    pub is_read_stream: bool,
    pub file: [::std::os::raw::c_char; 1024usize],
    pub use_stdio: bool,
    pub buf: *mut u8,
    pub buf_len: usize,
    pub buf_compressed_len: usize,
    pub read_fn: grk_stream_read_fn,
    pub write_fn: grk_stream_write_fn,
    pub seek_fn: grk_stream_seek_fn,
    pub free_user_data_fn: grk_stream_free_user_data_fn,
    pub user_data: *mut ::std::os::raw::c_void,
    pub stream_len: usize,
    pub username: [::std::os::raw::c_char; 129usize],
    pub password: [::std::os::raw::c_char; 129usize],
    pub bearer_token: [::std::os::raw::c_char; 4097usize],
    pub custom_headers: [[::std::os::raw::c_char; 1024usize]; 16usize],
    pub num_custom_headers: u8,
    pub region: [::std::os::raw::c_char; 64usize],
    pub s3_endpoint: [::std::os::raw::c_char; 256usize],
    pub s3_use_https: i8,
    pub s3_use_virtual_hosting: i8,
    pub s3_no_sign_request: bool,
    pub s3_allow_insecure: bool,
    pub cookie: [::std::os::raw::c_char; 4096usize],
    pub cookie_file: [::std::os::raw::c_char; 1024usize],
    pub cookie_jar: [::std::os::raw::c_char; 1024usize],
    pub netrc: bool,
    pub netrc_file: [::std::os::raw::c_char; 1024usize],
    pub proxy: [::std::os::raw::c_char; 512usize],
    pub proxy_userpwd: [::std::os::raw::c_char; 256usize],
    pub request_payer: [::std::os::raw::c_char; 64usize],
    pub user_agent: [::std::os::raw::c_char; 256usize],
    pub timeout: ::std::os::raw::c_long,
    pub connect_timeout: ::std::os::raw::c_long,
    pub max_retry: u32,
    pub retry_delay: u32,
    pub fetch_batch_size: u32,
    pub http2_multiplex: bool,
}
#[doc = " @struct grk_stream_params\n @brief JPEG 2000 stream parameters\n There are three methods of streaming: by file, buffer or callback\n Client must choose only one method, and populate this struct accordingly"]
pub type grk_stream_params = _grk_stream_params;
#[doc = " @struct grk_image\n @brief Grok image\n Note: do not directly create a grk_image object. Instead use the @ref grk_image_new\n API method to create a grk_image object, and clean it up with @ref grk_object_unref\n\n COORDINATE FRAMES:\n\n grk_image is used both as input to compression and as output from\n decompression. The interpretation of x0/y0/x1/y1 versus\n comps[i].x0/y0/w/h depends on which role the image is playing:\n\n 1. Compression input (caller-populated source image):\n      - x0/y0/x1/y1            = image bounds on the reference grid.\n      - comps[i].x0/y0 + (w,h) = the component's full extent, equal to\n                                 the image bounds (scaled by dx/dy for\n                                 subsampled components).\n      Both frames agree.\n\n 2. Full-image decompression output (from @ref grk_decompress_get_image):\n      - x0/y0/x1/y1            = reduced image bounds.\n      - comps[i].x0/y0 + (w,h) = the component's full decoded extent.\n      Both frames agree.\n\n 3. Per-tile decompression output\n    (from @ref grk_decompress_get_tile_image), windowed decompress\n    (dw_x0/dw_y0/dw_x1/dw_y1 set on grk_decompress_parameters):\n      - x0/y0/x1/y1            = the FULL tile rectangle on the canvas\n                                 (unreduced, non-windowed tile bounds).\n      - comps[i].x0/y0 + (w,h) = the cropped decode window — the\n                                 intersection of dw_* with this tile,\n                                 reduced if dw_reduced was set.\n      The two frames DISAGREE. The data layout — origin, width, height,\n      stride — is described entirely by comps[i]. Do NOT take origin\n      from image->x0/y0 and size from comps[i].w/h: that mixes frames\n      and yields a rectangle that misses the actual decoded pixels.\n\n Recommended access pattern (works in all three cases):\n     auto& c = img->comps[componentIndex];\n     // origin in canvas coords:    (c.x0, c.y0)\n     // valid data rectangle:       width c.w, height c.h\n     // row pitch in samples:       c.stride\n     // pixel at canvas (X, Y):     c.data[(Y - c.y0) * c.stride + (X - c.x0)]"]
pub type grk_image = _grk_image;
#[doc = " @brief Callback invoked when a horizontal band of the decompressed image is ready for writing.\n\n @param yBegin first row (inclusive) of the completed band\n @param yEnd   one-past-last row (exclusive) of the completed band\n @param image  pointer to the image containing the completed band data\n @param user_data user data"]
pub type grk_io_band_callback = ::std::option::Option<
    unsafe extern "C" fn(
        yBegin: u32,
        yEnd: u32,
        image: *mut grk_image,
        user_data: *mut ::std::os::raw::c_void,
    ) -> bool,
>;
#[doc = " @brief Callback called when decompression of a tile has completed"]
pub type grk_decompress_callback = ::std::option::Option<
    unsafe extern "C" fn(
        codec: *mut ::std::os::raw::c_void,
        tile_index: u16,
        tile_image: *mut grk_image,
        reduction: u8,
        user_data: *mut ::std::os::raw::c_void,
    ),
>;
#[doc = " @struct grk_decompress_core_params\n @brief Core decompression parameters"]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct _grk_decompress_core_params {
    #[doc = " Set the number of highest resolution levels to be discarded.\n The image resolution is effectively divided by 2 to the power of the number of discarded\n levels. The reduce factor is limited by the smallest total number of decomposition levels\n across tiles. If value is greater than zero, then the image is decoded to original dimension\n divided by 2^(reduce). If the value is equal to zero or not set, then the image is decompressed\n at full resolution"]
    pub reduce: u8,
    #[doc = " Set the maximum number of quality layers to decompress.\n If there are fewer quality layers than the specified number, all quality layers will be\n decompressed. If value is non-zero, then only these specified layers are decompressed.\n If value is zero or not set, then all the quality layers are decompressed"]
    pub layers_to_decompress: u16,
    pub tile_cache_strategy: u32,
    pub max_active_tiles: u16,
    pub disable_random_access_flags: u32,
    pub skip_allocate_composite: bool,
    pub io_buffer_callback: grk_io_pixels_callback,
    pub io_user_data: *mut ::std::os::raw::c_void,
    pub io_register_client_callback: grk_io_register_reclaim_callback,
    pub io_band_callback: grk_io_band_callback,
    pub io_band_user_data: *mut ::std::os::raw::c_void,
    #[doc = " Array of 0-based component indices to decode.\n Only these components will be fully decoded (T1, DWT, etc.).\n When MCT is active, components 0-2 will always be decoded if any of them is requested.\n If num_comps_to_decode is 0 or comps_to_decode is nullptr, all components are decoded.\n Memory is owned by the caller."]
    pub comps_to_decode: *mut u16,
    pub num_comps_to_decode: u16,
}
#[doc = " @struct grk_decompress_core_params\n @brief Core decompression parameters"]
pub type grk_decompress_core_params = _grk_decompress_core_params;
#[doc = " @struct grk_decompress_parameters\n @brief Decompression parameters"]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct _grk_decompress_params {
    pub core: grk_decompress_core_params,
    pub asynchronous: bool,
    pub simulate_synchronous: bool,
    pub decompress_callback: grk_decompress_callback,
    pub decompress_callback_user_data: *mut ::std::os::raw::c_void,
    pub infile: [::std::os::raw::c_char; 1024usize],
    pub outfile: [::std::os::raw::c_char; 1024usize],
    pub decod_format: GRK_CODEC_FORMAT,
    pub cod_format: GRK_SUPPORTED_FILE_FMT,
    pub dw_x0: f64,
    pub dw_x1: f64,
    pub dw_y0: f64,
    pub dw_y1: f64,
    pub dw_reduced: bool,
    pub tile_index: u16,
    #[doc = "Note: when using Grok API, the following parameters should be set\non the HeaderInfo struct before reading the header. Otherwise they\nwill be ignored"]
    pub color_space: GRK_COLOR_SPACE,
    pub force_rgb: bool,
    pub apply_palette: bool,
    pub upsample: bool,
    pub precision: *mut grk_precision,
    pub num_precision: u32,
    pub rescale: *mut grk_rescale,
    pub num_rescale: u32,
    pub split_by_component: bool,
    pub single_tile_decompress: bool,
    pub io_xml: bool,
    pub compression: u32,
    pub compression_level: u32,
    pub duration: u32,
    pub repeats: u32,
    pub num_threads: u32,
    pub kernel_build_options: u32,
    pub device_id: i32,
    pub user_data: *mut ::std::os::raw::c_void,
}
#[doc = " @struct grk_decompress_parameters\n @brief Decompression parameters"]
pub type grk_decompress_parameters = _grk_decompress_params;
pub const _grk_data_type_GRK_INT_32: _grk_data_type = 0;
pub const _grk_data_type_GRK_INT_16: _grk_data_type = 1;
pub const _grk_data_type_GRK_INT_8: _grk_data_type = 2;
pub const _grk_data_type_GRK_FLOAT: _grk_data_type = 3;
pub const _grk_data_type_GRK_DOUBLE: _grk_data_type = 4;
#[doc = " @brief Grok Data types\n Used to specify the actual data type of Grok image components\n"]
pub type _grk_data_type = ::std::os::raw::c_int;
#[doc = " @brief Grok Data types\n Used to specify the actual data type of Grok image components\n"]
pub use self::_grk_data_type as grk_data_type;
#[doc = " @struct grk_image_comp\n @brief Image component\n\n (x0, y0, w, h) is the authoritative description of the data rectangle\n in @ref data for all roles — compression input, full-image\n decompression output, and per-tile decompression output. For a\n per-tile image from a windowed decompress, it describes the cropped\n decode window (not the full tile), and disagrees with the enclosing\n grk_image's x0/y0/x1/y1. See the grk_image struct comment for the full\n breakdown."]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct _grk_image_comp {
    pub x0: u32,
    pub y0: u32,
    pub w: u32,
    pub stride: u32,
    pub h: u32,
    pub dx: u8,
    pub dy: u8,
    pub prec: u8,
    pub sgnd: bool,
    pub type_: GRK_CHANNEL_TYPE,
    pub association: GRK_CHANNEL_ASSOC,
    pub crg_x: u16,
    pub crg_y: u16,
    pub data_type: grk_data_type,
    pub data: *mut ::std::os::raw::c_void,
    pub owns_data: bool,
}
#[doc = " @struct grk_image_comp\n @brief Image component\n\n (x0, y0, w, h) is the authoritative description of the data rectangle\n in @ref data for all roles — compression input, full-image\n decompression output, and per-tile decompression output. For a\n per-tile image from a windowed decompress, it describes the cropped\n decode window (not the full tile), and disagrees with the enclosing\n grk_image's x0/y0/x1/y1. See the grk_image struct comment for the full\n breakdown."]
pub type grk_image_comp = _grk_image_comp;
#[doc = " @struct grk_color\n @brief ICC profile, palette, channel definition"]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct _grk_color {
    pub icc_profile_buf: *mut u8,
    pub icc_profile_len: u32,
    pub icc_profile_name: *mut ::std::os::raw::c_char,
    pub channel_definition: *mut grk_channel_definition,
    pub palette: *mut grk_palette_data,
    pub has_colour_specification_box: bool,
}
#[doc = " @struct grk_color\n @brief ICC profile, palette, channel definition"]
pub type grk_color = _grk_color;
#[doc = " @struct grk_image_data\n @brief Image meta data: colour, IPTC, XMP and EXIF"]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct _grk_image_meta {
    pub obj: grk_object,
    pub color: grk_color,
    pub iptc_buf: *mut u8,
    pub iptc_len: usize,
    pub xmp_buf: *mut u8,
    pub xmp_len: usize,
    pub exif_buf: *mut u8,
    pub exif_len: usize,
    pub geotiff_buf: *mut u8,
    pub geotiff_len: usize,
    pub ipr_data: *mut u8,
    pub ipr_len: usize,
    pub xml_buf: *mut u8,
    pub xml_len: usize,
    pub asoc_boxes: *mut grk_asoc,
    pub num_asoc_boxes: u32,
}
#[doc = " @struct grk_image_data\n @brief Image meta data: colour, IPTC, XMP and EXIF"]
pub type grk_image_meta = _grk_image_meta;
#[doc = " @struct grk_image\n @brief Grok image\n Note: do not directly create a grk_image object. Instead use the @ref grk_image_new\n API method to create a grk_image object, and clean it up with @ref grk_object_unref\n\n COORDINATE FRAMES:\n\n grk_image is used both as input to compression and as output from\n decompression. The interpretation of x0/y0/x1/y1 versus\n comps[i].x0/y0/w/h depends on which role the image is playing:\n\n 1. Compression input (caller-populated source image):\n      - x0/y0/x1/y1            = image bounds on the reference grid.\n      - comps[i].x0/y0 + (w,h) = the component's full extent, equal to\n                                 the image bounds (scaled by dx/dy for\n                                 subsampled components).\n      Both frames agree.\n\n 2. Full-image decompression output (from @ref grk_decompress_get_image):\n      - x0/y0/x1/y1            = reduced image bounds.\n      - comps[i].x0/y0 + (w,h) = the component's full decoded extent.\n      Both frames agree.\n\n 3. Per-tile decompression output\n    (from @ref grk_decompress_get_tile_image), windowed decompress\n    (dw_x0/dw_y0/dw_x1/dw_y1 set on grk_decompress_parameters):\n      - x0/y0/x1/y1            = the FULL tile rectangle on the canvas\n                                 (unreduced, non-windowed tile bounds).\n      - comps[i].x0/y0 + (w,h) = the cropped decode window — the\n                                 intersection of dw_* with this tile,\n                                 reduced if dw_reduced was set.\n      The two frames DISAGREE. The data layout — origin, width, height,\n      stride — is described entirely by comps[i]. Do NOT take origin\n      from image->x0/y0 and size from comps[i].w/h: that mixes frames\n      and yields a rectangle that misses the actual decoded pixels.\n\n Recommended access pattern (works in all three cases):\n     auto& c = img->comps[componentIndex];\n     // origin in canvas coords:    (c.x0, c.y0)\n     // valid data rectangle:       width c.w, height c.h\n     // row pitch in samples:       c.stride\n     // pixel at canvas (X, Y):     c.data[(Y - c.y0) * c.stride + (X - c.x0)]"]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct _grk_image {
    pub _address: u8,
}
#[doc = " @struct grk_header_info\n @brief JPEG 2000 header info"]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct _grk_header_info {
    #[doc = "Variables below are populated by library after reading header"]
    pub header_image: grk_image,
    pub cblockw_init: u32,
    pub cblockh_init: u32,
    pub irreversible: bool,
    pub mct: u8,
    pub rsiz: u16,
    pub numresolutions: u8,
    pub prog_order: GRK_PROG_ORDER,
    #[doc = "coding style\nCan be specified in main header COD segment,\ntile header COD segment, and tile component COC segment.\nImportant note: we assume that coding style does not vary across tile components"]
    pub csty: u8,
    #[doc = "code block style\nCan be specified in main header COD segment, and can   be overridden in a tile header.\nImportant note: we assume that code block style does not vary across tiles"]
    pub cblk_sty: u8,
    pub prcw_init: [u32; 33usize],
    pub prch_init: [u32; 33usize],
    pub tx0: u32,
    pub ty0: u32,
    pub t_width: u32,
    pub t_height: u32,
    pub t_grid_width: u16,
    pub t_grid_height: u16,
    pub num_layers: u16,
    pub xml_data: *mut u8,
    pub xml_data_len: usize,
    pub xml_boxes_data: [*mut u8; 64usize],
    pub xml_boxes_len: [usize; 64usize],
    pub num_xml_boxes: u32,
    pub num_comments: usize,
    pub comment: [*mut ::std::os::raw::c_char; 256usize],
    pub comment_len: [u16; 256usize],
    pub is_binary_comment: [bool; 256usize],
    pub asocs: [grk_asoc; 256usize],
    pub num_asocs: u32,
    #[doc = "Variables below are set by client"]
    pub color_space: GRK_COLOR_SPACE,
    pub decompress_fmt: GRK_SUPPORTED_FILE_FMT,
    pub force_rgb: bool,
    pub apply_palette: bool,
    pub upsample: bool,
    pub precision: *mut grk_precision,
    pub num_precision: u32,
    pub rescale: *mut grk_rescale,
    pub num_rescale: u32,
    pub split_by_component: bool,
    pub single_tile_decompress: bool,
}
#[doc = " @struct grk_header_info\n @brief JPEG 2000 header info"]
pub type grk_header_info = _grk_header_info;
#[doc = " @struct grk_swath_buffer\n @brief User-managed output buffer for asynchronous swath tile copy-and-convert.\n\n After grk_decompress_wait() returns for a given swath, pass this struct to\n grk_decompress_schedule_swath_copy() to schedule per-tile copies from the\n internal int32_t tile buffers into the caller's output buffer via Taskflow\n and Highway SIMD.\n\n The output buffer layout mirrors GDAL's pixel/line/band spacing convention,\n supporting BIP, BSQ, or any other interleaving.  Output type is described by\n prec (bit depth: 8, 16, or 32) and sgnd (signed vs unsigned):\n   prec=8,  sgnd=false  → uint8_t   (GDT_Byte)\n   prec=16, sgnd=false  → uint16_t  (GDT_UInt16)\n   prec=16, sgnd=true   → int16_t   (GDT_Int16)\n   prec=32, sgnd=false  → uint32_t  (GDT_UInt32)\n   prec=32, sgnd=true   → int32_t   (GDT_Int32)\n\n Conversion from the internal int32_t source follows GDALCopyWords\n semantics: values are clamped to the output type's representable range.\n\n band_map[i] is a 1-based component index (GDAL panBandMap convention):\n output band i is sourced from image component band_map[i]-1.\n If band_map is NULL, band i is sourced from component i.\n\n promote_alpha: if >= 0, the component at this 0-based index is a 1-bit\n alpha channel and its values are scaled by 255 before output.\n\n Assumes component subsampling dx == dy == 1.\n The user must keep the buffer alive until grk_decompress_wait_swath_copy()."]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct grk_swath_buffer {
    #[doc = "< Output buffer pointer"]
    pub data: *mut ::std::os::raw::c_void,
    #[doc = "< Output bit depth: 8, 16, or 32"]
    pub prec: u8,
    #[doc = "< true = signed output (int16/int32); false = unsigned"]
    pub sgnd: bool,
    #[doc = "< Number of output bands"]
    pub numcomps: u16,
    #[doc = "< Bytes between adjacent pixels in same row and band (GDAL nPixelSpace)"]
    pub pixel_space: i64,
    #[doc = "< Bytes between adjacent rows in same band (GDAL nLineSpace)"]
    pub line_space: i64,
    #[doc = "< Bytes between adjacent bands (GDAL nBandSpace)"]
    pub band_space: i64,
    #[doc = "< numcomps-element array of 1-based component indices, or NULL"]
    pub band_map: *mut ::std::os::raw::c_int,
    #[doc = "< 0-based component index for 1-bit alpha promotion, or -1"]
    pub promote_alpha: ::std::os::raw::c_int,
    #[doc = "< Swath image x origin (image pixel coordinates)"]
    pub x0: u32,
    #[doc = "< Swath image y origin (image pixel coordinates)"]
    pub y0: u32,
    #[doc = "< Swath image x end, exclusive (image pixel coordinates)"]
    pub x1: u32,
    #[doc = "< Swath image y end, exclusive (image pixel coordinates)"]
    pub y1: u32,
}
#[doc = " @struct grk_wait_swath\n @brief Specify swath region to wait on during asynchronous decompression.\n\n Input: Set pixel coordinates (x0, y0, x1, y1) before calling grk_decompress_wait().\n Output: After grk_decompress_wait() returns, tile_x0/y0/x1/y1 and num_tile_cols\n         are populated with the tile grid indices covering the requested swath.\n         These can be used with grk_decompress_get_tile_image() to retrieve\n         per-tile decoded data.\n\n Note: tile coordinates are only populated when grk_decompress_wait() is called\n with a non-null swath pointer and asynchronous+simulate_synchronous mode is enabled."]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct grk_wait_swath {
    #[doc = "< Input: Pixel coordinate: top-left x"]
    pub x0: u32,
    #[doc = "< Input: Pixel coordinate: top-left y"]
    pub y0: u32,
    #[doc = "< Input: Pixel coordinate: bottom-right x (exclusive)"]
    pub x1: u32,
    #[doc = "< Input: Pixel coordinate: bottom-right y (exclusive)"]
    pub y1: u32,
    #[doc = "< Output: Global tile column start (inclusive)"]
    pub tile_x0: u16,
    #[doc = "< Output: Global tile row start (inclusive)"]
    pub tile_y0: u16,
    #[doc = "< Output: Global tile column end (exclusive)"]
    pub tile_x1: u16,
    #[doc = "< Output: Global tile row end (exclusive)"]
    pub tile_y1: u16,
    #[doc = "< Output: Total tile columns in the image tile grid"]
    pub num_tile_cols: u16,
}
#[doc = " @struct grk_plugin_pass\n @brief Plugin pass"]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct _grk_plugin_pass {
    pub distortion_decrease: f64,
    pub rate: usize,
    pub length: usize,
}
#[doc = " @struct grk_plugin_pass\n @brief Plugin pass"]
pub type grk_plugin_pass = _grk_plugin_pass;
#[doc = " @struct grk_plugin_code_block\n @brief Plugin code block"]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct _grk_plugin_code_block {
    #[doc = "debug info"]
    pub x0: u32,
    #[doc = "debug info"]
    pub y0: u32,
    #[doc = "debug info"]
    pub x1: u32,
    #[doc = "debug info"]
    pub y1: u32,
    pub context_stream: *mut ::std::os::raw::c_uint,
    pub num_pix: u32,
    pub compressed_data: *mut u8,
    pub compressed_data_length: u32,
    pub num_bit_planes: u8,
    pub num_passes: u8,
    pub passes: [grk_plugin_pass; 67usize],
    pub sorted_index: ::std::os::raw::c_uint,
}
#[doc = " @struct grk_plugin_code_block\n @brief Plugin code block"]
pub type grk_plugin_code_block = _grk_plugin_code_block;
#[doc = " @brief grk_plugin_precinct\n @brief Plugin precinct"]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct _grk_plugin_precinct {
    pub num_blocks: u64,
    pub blocks: *mut *mut grk_plugin_code_block,
}
#[doc = " @brief grk_plugin_precinct\n @brief Plugin precinct"]
pub type grk_plugin_precinct = _grk_plugin_precinct;
#[doc = " @struct grk_plugin_band\n @brief Plugin band"]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct _grk_plugin_band {
    pub orientation: u8,
    pub num_precincts: u64,
    pub precincts: *mut *mut grk_plugin_precinct,
    pub stepsize: f32,
}
#[doc = " @struct grk_plugin_band\n @brief Plugin band"]
pub type grk_plugin_band = _grk_plugin_band;
#[doc = " @struct grk_plugin_resolution\n @brief Plugin resolution"]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct _grk_plugin_resolution {
    pub level: u8,
    pub num_bands: u8,
    pub band: *mut *mut grk_plugin_band,
}
#[doc = " @struct grk_plugin_resolution\n @brief Plugin resolution"]
pub type grk_plugin_resolution = _grk_plugin_resolution;
#[doc = " @struct grk_plugin_tile_component\n @brief Plugin tile component"]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct grk_plugin_tile_component {
    pub numresolutions: u8,
    pub resolutions: *mut *mut grk_plugin_resolution,
}
#[doc = " @struct grk_plugin_tile\n @brief Plugin tile"]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct _grk_plugin_tile {
    pub decompress_flags: u32,
    pub num_components: u16,
    pub tile_components: *mut *mut grk_plugin_tile_component,
}
#[doc = " @struct grk_plugin_tile\n @brief Plugin tile"]
pub type grk_plugin_tile = _grk_plugin_tile;
extern "C" {
    #[doc = " @brief Gets the Grok library version string.\n\n Returns a null-terminated string of the form \"MAJOR.MINOR.PATCH\"\n (e.g. \"10.0.5\").  The string is statically allocated; do not free it.\n\n @return null-terminated version string"]
    pub fn grk_version() -> *const ::std::os::raw::c_char;
}
extern "C" {
    #[doc = " @brief Detect the JPEG 2000 codec format of a file by reading its magic bytes.\n\n Reads the first bytes of the file and returns the detected format:\n - GRK_CODEC_JP2 for JP2 or JPH container files\n - GRK_CODEC_J2K for raw codestream files (J2K, J2C, JPC)\n - GRK_CODEC_MJ2 for Motion JPEG 2000 files\n\n @param file_path  path to the file to inspect\n @param format     [out] detected codec format\n @return true if a valid JPEG 2000 format was detected, false otherwise"]
    pub fn grk_detect_format(
        file_path: *const ::std::os::raw::c_char,
        format: *mut GRK_CODEC_FORMAT,
    ) -> bool;
}
extern "C" {
    #[doc = " @brief Initializes the Grok library.\n\n Must be called once before any other Grok API function.  It is safe to call\n multiple times; subsequent calls are no-ops unless the library was shut down.\n\n Sets up the Taskflow thread pool used by all async decompress/compress\n operations. Pass @p num_threads = 0 to use all available logical CPUs.\n The thread pool persists for the lifetime of the process.\n\n @param plugin_path       path to an optional hardware-accelerator plugin .so;\n                          pass NULL for CPU-only operation\n @param num_threads       number of worker threads (0 = use all CPUs)\n @param plugin_initialized if non-NULL, set to true when a plugin was loaded\n                          and initialized successfully, false otherwise"]
    pub fn grk_initialize(
        plugin_path: *const ::std::os::raw::c_char,
        num_threads: u32,
        plugin_initialized: *mut bool,
    );
}
extern "C" {
    #[doc = " @brief Deinitialize the Grok library.\n\n Cleans up the plugin and thread pool. Call at the end of main()\n to ensure GPU resources are freed before CUDA runtime unloads."]
    pub fn grk_deinitialize();
}
extern "C" {
    #[doc = " @brief Increments the reference count on a Grok object.\n\n Call this when you want to share ownership of a codec or image object\n across multiple owners.  Each call to grk_object_ref() must be matched\n by exactly one call to grk_object_unref().\n\n @param obj Grok object (see @ref grk_object); passing NULL is a no-op\n @return the same @p obj pointer (for convenience), or NULL if obj is NULL"]
    pub fn grk_object_ref(obj: *mut grk_object) -> *mut grk_object;
}
extern "C" {
    #[doc = " @brief Decrements the reference count on a Grok object.\n\n When the reference count reaches zero the object (codec or image) is\n destroyed and all associated decompressor/compressor resources are freed.\n The caller must not access the object after this call if its count reaches\n zero.\n\n For a decompressor codec: completes any in-flight async tasks before\n destroying the object.  For an image: frees component data buffers.\n\n @param obj Grok object (see @ref grk_object); passing NULL is a no-op"]
    pub fn grk_object_unref(obj: *mut grk_object);
}
extern "C" {
    #[doc = " @brief Installs application-defined log message handlers.\n\n Replaces the default handlers (which print to stderr/stdout) with\n user-supplied callbacks for info, warning, and error messages.\n Call before grk_initialize() or at any point to change handlers.\n Passing a NULL function pointer for any handler restores the default\n behaviour for that level.\n\n @param msg_handlers struct of {info, warn, error} function pointers\n                     (see @ref grk_msg_handlers)"]
    pub fn grk_set_msg_handlers(msg_handlers: grk_msg_handlers);
}
extern "C" {
    #[doc = " @brief Allocates a new Grok image with the specified component layout.\n\n Creates an image whose component geometry is described by @p cmptparms.\n Each entry in @p cmptparms specifies width, height, bit-depth, signedness,\n and sub-sampling factors for one component.\n\n When @p alloc_data is true, a contiguous int32_t data block is allocated\n for every component; when false, the component data pointers are NULL and\n the caller must supply them before passing the image to grk_compress_init().\n\n The returned image is reference-counted.  Release with grk_object_unref().\n\n @param numcmpts   number of components (e.g. 1 for grayscale, 3 for RGB)\n @param cmptparms  per-component parameters array — length must be @p numcmpts\n                   (see @ref grk_image_comp)\n @param clrspc     image colour space (e.g. GRK_CLRSPC_SRGB, GRK_CLRSPC_GRAY\n                   — see @ref GRK_COLOR_SPACE)\n @param alloc_data if true, allocate int32_t data buffers for all components\n @return pointer to newly allocated @ref grk_image, or NULL on failure"]
    pub fn grk_image_new(
        numcmpts: u16,
        cmptparms: *mut grk_image_comp,
        clrspc: GRK_COLOR_SPACE,
        alloc_data: bool,
    ) -> *mut grk_image;
}
extern "C" {
    #[doc = " @brief Determine the data type used by Grok for image component data.\n\n Returns the data type (GRK_INT_32 or GRK_INT_16) that Grok will use\n for storing image component data during compression or decompression,\n assuming standard conditions (whole-tile decoding, num_resolutions > 1).\n\n @param compress  true for compression, false for decompression\n @param prec      image component precision in bits\n @param is_mct    true if multi-component transform (MCT/RCT/ICT) is applied\n @param qmfbid    wavelet transform: 1 for reversible (5/3), 0 for irreversible (9/7)\n @return GRK_INT_16 if the 16-bit path will be used, GRK_INT_32 otherwise"]
    pub fn grk_get_data_type(compress: bool, prec: u8, is_mct: bool, qmfbid: u8) -> grk_data_type;
}
extern "C" {
    #[doc = " @brief Allocates an empty image metadata object.\n\n Returns a heap-allocated @ref grk_image_meta that holds optional metadata\n (ICC profile, XMP, IPTC, EXIF) to associate with an image before\n compression.  Free with grk_object_unref() when done.\n\n @return pointer to newly allocated @ref grk_image_meta, or NULL on OOM"]
    pub fn grk_image_meta_new() -> *mut grk_image_meta;
}
extern "C" {
    #[doc = " @brief Set a metadata buffer on a grk_image_meta object.\n\n Copies @p len bytes from @p data into the specified metadata field,\n replacing any existing data.  The previous buffer (if any) is freed.\n\n @param meta   pointer to an existing grk_image_meta\n @param field  which metadata field to set:\n               \"geotiff\", \"ipr\", \"xmp\", \"iptc\", \"exif\", \"xml\"\n @param data   pointer to source bytes (copied internally)\n @param len    number of bytes to copy\n @return true on success, false on invalid field name or OOM"]
    pub fn grk_image_meta_set_field(
        meta: *mut grk_image_meta,
        field: *const ::std::os::raw::c_char,
        data: *const u8,
        len: usize,
    ) -> bool;
}
extern "C" {
    #[doc = " @brief Get a metadata buffer from a grk_image_meta object.\n\n Returns a pointer to the internal buffer for the specified metadata field.\n The pointer is valid for the lifetime of the grk_image_meta object.\n\n @param meta      pointer to an existing grk_image_meta\n @param field     which metadata field to get:\n                  \"geotiff\", \"ipr\", \"xmp\", \"iptc\", \"exif\", \"xml\"\n @param[out] data set to internal buffer pointer (do not free)\n @param[out] len  set to buffer length\n @return true on success (even if data is NULL/empty), false on invalid field"]
    pub fn grk_image_meta_get_field(
        meta: *mut grk_image_meta,
        field: *const ::std::os::raw::c_char,
        data: *mut *mut u8,
        len: *mut usize,
    ) -> bool;
}
extern "C" {
    #[doc = " @brief Set association boxes on a grk_image_meta object for writing.\n\n The asoc array uses a flattened tree representation:\n - Level 0 entries are top-level association boxes\n - Level N entries are children of the most recent level N-1 entry\n Each entry has a label and optionally XML content.\n\n This is used for GMLJP2 metadata.  The data is copied internally.\n\n @param meta       pointer to an existing grk_image_meta\n @param asocs      array of association box entries\n @param num_asocs  number of entries in the array\n @return true on success, false on OOM or invalid input"]
    pub fn grk_image_meta_set_asocs(
        meta: *mut grk_image_meta,
        asocs: *const grk_asoc,
        num_asocs: u32,
    ) -> bool;
}
extern "C" {
    #[doc = " @brief Creates and initializes a JPEG 2000 decompressor.\n\n Opens the source stream and reads enough of the header to determine the\n codec type (J2K codestream or JP2 file-format).  The returned object holds\n all decompressor state and must be released with grk_object_unref() when\n the caller is done with it.\n\n This function does NOT read the full image header; call\n grk_decompress_read_header() next to populate @ref grk_header_info.\n\n @param stream_params  input stream description (file path, buffer, or\n                       callbacks — see @ref grk_stream_params)\n @param params         decompression options (tile range, window region,\n                       async mode, etc. — see @ref grk_decompress_parameters)\n @return pointer to an opaque @ref grk_object on success, NULL on failure\n         (bad stream, unsupported format, OOM)"]
    pub fn grk_decompress_init(
        stream_params: *mut grk_stream_params,
        params: *mut grk_decompress_parameters,
    ) -> *mut grk_object;
}
extern "C" {
    #[doc = " @brief Updates decompression parameters on an already-initialized codec.\n\n Allows changing parameters (e.g. decoding region, reduce factor,\n quality layers) after grk_decompress_init() but before the first\n grk_decompress() call.  Not all parameters can be changed mid-stream;\n refer to @ref grk_decompress_parameters for which fields are live-updatable.\n\n @param params updated decompression parameters (see @ref grk_decompress_parameters)\n @param codec  decompression codec to update (see @ref grk_object)\n @return true if parameters were applied successfully, false otherwise"]
    pub fn grk_decompress_update(
        params: *mut grk_decompress_parameters,
        codec: *mut grk_object,
    ) -> bool;
}
extern "C" {
    #[doc = " @brief Retrieves the current progression state for a cached tile.\n\n A progression state describes how many quality layers have been decoded\n for each resolution level in a given tile.  This is used to implement\n incremental / partial-quality decompression: decode a tile at reduced\n quality, inspect it, then decode more layers on demand.\n\n Returns a zero-initialised struct if the tile has not been decompressed\n yet or is not in the tile cache.\n\n @param codec       decompression codec (see @ref grk_object)\n @param tile_index  zero-based tile index (row-major within the tile grid)\n @return current @ref grk_progression_state for the tile; all-zeros if\n         the tile has not been decoded"]
    pub fn grk_decompress_get_progression_state(
        codec: *mut grk_object,
        tile_index: u16,
    ) -> grk_progression_state;
}
extern "C" {
    #[doc = " @brief Applies a new progression state to one or all cached tiles.\n\n When @p state.single_tile is true, only the tile at @p state.tile_index\n is updated.  The maximum number of quality layers to decode for each\n resolution level is taken from @p state.layers_per_resolution.\n If the new limit differs from the current one, the tile is marked dirty\n so that a subsequent grk_decompress() call re-decodes it with the new\n layer budget.\n\n Typical use: decode a full swath quickly with 1 layer, then apply more\n layers on demand for specific tiles.\n\n @param codec  decompression codec (see @ref grk_object)\n @param state  desired progression state (see @ref grk_progression_state);\n               state.single_tile must be true (multi-tile update not supported)\n @return true if the tile was found in the cache and the state was stored;\n         false if single_tile is false, the tile is not cached, or the\n         codec has no decompressor"]
    pub fn grk_decompress_set_progression_state(
        codec: *mut grk_object,
        state: grk_progression_state,
    ) -> bool;
}
extern "C" {
    #[doc = " @brief Reads and parses the JPEG 2000 image header.\n\n Must be called after grk_decompress_init() and before grk_decompress().\n Populates @p header_info with image dimensions, tile grid, component\n descriptions, color space, bit-depths, ICC profile pointer, and\n progression/quality layer counts.\n\n For asynchronous decoding: the image dimensions reported here reflect\n the full unscaled image; the async scheduler uses them when computing\n tile ranges for grk_decompress_wait().\n\n @param codec        decompression codec (see @ref grk_object)\n @param header_info  caller-allocated struct to receive header data\n                     (see @ref grk_header_info); may be NULL if the caller\n                     only needs to prime the codec for grk_decompress()\n @return true if the codestream main header (and JP2 superbox, if present)\n         were read successfully; false on I/O or parse error"]
    pub fn grk_decompress_read_header(
        codec: *mut grk_object,
        header_info: *mut grk_header_info,
    ) -> bool;
}
extern "C" {
    #[doc = " @brief Gets decompressed tile image by tile index.\n\n Returns per-tile decoded data after decompression completes.\n Works for both single-tile and multi-tile images.\n The tile index is: tile_y * num_tile_cols + tile_x\n (use grk_wait_swath output fields to compute).\n\n @param\tcodec\t\tdecompression codec (see @ref grk_object)\n @param\ttile_index\ttile index (row-major within the image tile grid)\n @param\twait\t\tif true, block until the specified tile is decompressed\n @return pointer to @ref grk_image for the tile, or NULL if tile not available"]
    pub fn grk_decompress_get_tile_image(
        codec: *mut grk_object,
        tile_index: u16,
        wait: bool,
    ) -> *mut grk_image;
}
extern "C" {
    #[doc = " @brief Gets the composite decompressed image.\n\n Returns the composited output image (all tiles merged).\n Only valid after full decompression completes (grk_decompress_wait with null swath).\n Requires skip_allocate_composite=false in decompress parameters for data to be present.\n For per-tile access, prefer grk_decompress_get_tile_image() instead.\n\n @param\tcodec\tdecompression codec (see @ref grk_object)\n @return pointer to @ref grk_image"]
    pub fn grk_decompress_get_image(codec: *mut grk_object) -> *mut grk_image;
}
extern "C" {
    #[doc = " @brief Set a band-completion callback on the decompressor.\n\n Must be called after grk_decompress_read_header() but before grk_decompress().\n When set, the decompressor invokes the callback as each tile row completes,\n enabling incremental writing of decompressed image bands.\n\n @param codec   decompressor object\n @param callback function to invoke per completed band\n @param user_data opaque pointer forwarded to the callback"]
    pub fn grk_decompress_set_band_callback(
        codec: *mut grk_object,
        callback: grk_io_band_callback,
        user_data: *mut ::std::os::raw::c_void,
    );
}
extern "C" {
    #[doc = " @brief Checks whether the image requires post-processing (palette, ICC, colour\n space conversion, precision override, or upsampling).\n\n @param image pointer to the decompressed image\n @return true if all post-processing steps are no-ops"]
    pub fn grk_image_is_post_process_no_op(image: *mut grk_image) -> bool;
}
extern "C" {
    #[doc = " @brief Starts (or continues) decompression of the JPEG 2000 image.\n\n For synchronous decoding (asynchronous = false): blocks until all\n requested tiles are decompressed.  After return, component data is\n available via grk_decompress_get_image() or grk_decompress_get_tile_image().\n\n For asynchronous decoding (asynchronous = true): returns immediately\n after scheduling decompression tasks on the Taskflow executor.  Use\n grk_decompress_wait() to synchronize on individual swath regions, and\n grk_decompress_schedule_swath_copy() + grk_decompress_wait_swath_copy()\n to copy decoded data into an output buffer.\n\n The @p tile parameter is only used when integrating a hardware-\n accelerator plugin; pass NULL for CPU-only decoding.\n\n @param codec  decompression codec (see @ref grk_object)\n @param tile   plugin tile data, or NULL for CPU-only decoding\n               (see @ref grk_plugin_tile)\n @return true if successful (sync) or if scheduling succeeded (async)"]
    pub fn grk_decompress(codec: *mut grk_object, tile: *mut grk_plugin_tile) -> bool;
}
extern "C" {
    #[doc = " @brief Waits for an asynchronous decompression to complete.\n\n If swath is non-null: waits for all tiles covering the swath region,\n then populates swath->tile_x0/y0/x1/y1 and swath->num_tile_cols with\n the tile grid indices. Use these with grk_decompress_get_tile_image()\n to retrieve per-tile data.\n\n If swath is null: waits for the entire decompression to complete\n (all tiles, all post-processing). No tile coordinates are output.\n\n @param codec codec @ref grk_object\n @param swath @ref grk_wait_swath to wait for, or NULL for full wait"]
    pub fn grk_decompress_wait(codec: *mut grk_object, swath: *mut grk_wait_swath);
}
extern "C" {
    #[doc = " @brief Schedule tile-to-swath copies for a completed swath.\n\n Call after grk_decompress_wait() returns for the given swath. For each tile\n in the swath, a Taskflow task is submitted to the library executor that\n converts the internal int32_t planar tile data into the user-supplied output\n buffer using Highway SIMD (clamp + right-shift to target precision).\n\n Tiles whose decompression Taskflow future is still in flight (ahead of the\n current swath due to parallel scheduling) have their copy task chained as a\n Taskflow continuation, so the copy runs as soon as the tile is ready.\n\n The output buffer (@ref grk_swath_buffer) uses BSQ layout (one plane per\n component). Call grk_decompress_wait_swath_copy() to wait for all scheduled\n copies before accessing buf->data.\n\n @param codec codec @ref grk_object\n @param swath swath descriptor with tile_x0/y0/x1/y1 populated by grk_decompress_wait\n @param buf   user-managed output buffer; must stay valid until\n              grk_decompress_wait_swath_copy() returns"]
    pub fn grk_decompress_schedule_swath_copy(
        codec: *mut grk_object,
        swath: *const grk_wait_swath,
        buf: *mut grk_swath_buffer,
    );
}
extern "C" {
    #[doc = " @brief Wait for all in-flight swath copy tasks to complete.\n\n Blocks until every copy task submitted by grk_decompress_schedule_swath_copy()\n has finished. After this returns, buf->data is fully populated and safe to access.\n\n @param codec codec @ref grk_object"]
    pub fn grk_decompress_wait_swath_copy(codec: *mut grk_object);
}
extern "C" {
    #[doc = " @brief Copy a single decoded tile image into a swath buffer using Highway SIMD.\n\n A low-level, synchronous alternative to grk_decompress_schedule_swath_copy()\n for callers that manage their own threading.  Copies the source component\n data from @p tile_img into @p buf, clipping to buf's x/y window, applying\n band_map re-ordering, alpha promotion, and type conversion (int32 → prec/sgnd).\n\n Thread-safe: multiple tiles may be copied into the same buf concurrently\n provided they write to non-overlapping output regions.\n\n @param tile_img  tile image returned by grk_decompress_get_tile_image()\n @param buf       output swath buffer (must be pre-allocated and fully populated)"]
    pub fn grk_copy_tile_to_swath(tile_img: *const grk_image, buf: *const grk_swath_buffer);
}
extern "C" {
    #[doc = " @brief Decompresses a single tile by index.\n\n Decodes only the tile at @p tile_index without decompressing the entire\n image.  Useful for random-access workflows where only a subset of tiles\n is needed.  The tile index is row-major: tile_y * num_tile_cols + tile_x.\n\n After this returns, retrieve the decoded image via\n grk_decompress_get_tile_image(codec, tile_index, false).\n\n @param codec       decompression codec (see @ref grk_object)\n @param tile_index  zero-based tile index (row-major within the tile grid)\n @return true if the tile was decoded successfully, false on error"]
    pub fn grk_decompress_tile(codec: *mut grk_object, tile_index: u16) -> bool;
}
extern "C" {
    #[doc = " @brief Gets the number of samples (frames) in the codec container.\n For single-image formats (JP2, J2K) this returns 1.\n For multi-frame formats (MJ2) this returns the number of video samples.\n @param\tcodec\tdecompression codec (see @ref grk_object)\n @return number of samples"]
    pub fn grk_decompress_num_samples(codec: *mut grk_object) -> u32;
}
extern "C" {
    #[doc = " @brief Decompresses a single sample (frame) by index.\n @param\tcodec\t\t\tdecompression codec (see @ref grk_object)\n @param\tsample_index\tsample index (0-based)\n @return true if successful, otherwise false"]
    pub fn grk_decompress_sample(codec: *mut grk_object, sample_index: u32) -> bool;
}
extern "C" {
    #[doc = " @brief Gets the decompressed image for a specific sample (frame).\n @param\tcodec\t\t\tdecompression codec (see @ref grk_object)\n @param\tsample_index\tsample index (0-based)\n @return pointer to @ref grk_image, or NULL if sample not decompressed"]
    pub fn grk_decompress_get_sample_image(
        codec: *mut grk_object,
        sample_index: u32,
    ) -> *mut grk_image;
}
extern "C" {
    #[doc = " @brief Gets a decompressed tile image from a specific sample (frame).\n Useful for multi-frame containers (MJ2) where each frame may have multiple tiles.\n The sample must have been decompressed first via grk_decompress() (for sample 0)\n or grk_decompress_sample() (for subsequent samples).\n @param\tcodec\t\t\tdecompression codec (see @ref grk_object)\n @param\tsample_index\tsample index (0-based)\n @param\ttile_index\t\ttile index within the sample (row-major)\n @return pointer to @ref grk_image for the tile, or NULL if not available"]
    pub fn grk_decompress_get_sample_tile_image(
        codec: *mut grk_object,
        sample_index: u32,
        tile_index: u16,
    ) -> *mut grk_image;
}
#[doc = " @struct grk_cparameters\n @brief Compression parameters"]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct _grk_cparameters {
    pub tile_size_on: bool,
    pub tx0: u32,
    pub ty0: u32,
    pub t_width: u32,
    pub t_height: u32,
    pub numlayers: u16,
    #[doc = " rate control allocation by rate/distortion curve"]
    pub allocation_by_rate_distortion: bool,
    #[doc = " layers rates expressed as compression ratios.\n  They might be subsequently limited by the max_cs_size field"]
    pub layer_rate: [f64; 256usize],
    pub allocation_by_quality: bool,
    pub layer_distortion: [f64; 256usize],
    pub comment: [*mut ::std::os::raw::c_char; 256usize],
    pub comment_len: [u16; 256usize],
    pub is_binary_comment: [bool; 256usize],
    pub num_comments: usize,
    pub csty: u8,
    pub numgbits: u8,
    pub prog_order: GRK_PROG_ORDER,
    pub progression: [grk_progression; 33usize],
    pub numpocs: u32,
    pub numresolution: u8,
    pub cblockw_init: u32,
    pub cblockh_init: u32,
    pub cblk_sty: u8,
    pub irreversible: bool,
    #[doc = " region of interest: affected component in [0..3];\n  -1 indicates no ROI"]
    pub roi_compno: i32,
    pub roi_shift: u32,
    pub res_spec: u32,
    pub prcw_init: [u32; 33usize],
    pub prch_init: [u32; 33usize],
    pub infile: [::std::os::raw::c_char; 1024usize],
    pub outfile: [::std::os::raw::c_char; 1024usize],
    pub image_offset_x0: u32,
    pub image_offset_y0: u32,
    pub subsampling_dx: u8,
    pub subsampling_dy: u8,
    pub decod_format: GRK_SUPPORTED_FILE_FMT,
    pub cod_format: GRK_SUPPORTED_FILE_FMT,
    pub raw_cp: grk_raw_cparameters,
    pub enable_tile_part_generation: bool,
    pub new_tile_part_progression_divider: u8,
    pub mct: u8,
    #[doc = " Naive implementation of MCT restricted to a single reversible array based\ncompressing without offset concerning all the components."]
    pub mct_data: *mut ::std::os::raw::c_void,
    #[doc = " Maximum size (in bytes) for the whole code stream.\n If equal to zero, code stream size limitation is not considered\n If it does not comply with layer_rate, max_cs_size prevails\n and a warning is issued."]
    pub max_cs_size: u64,
    #[doc = " Maximum size (in bytes) for each component.\n If == 0, component size limitation is not considered"]
    pub max_comp_size: u64,
    #[doc = " RSIZ value\nTo be used to combine GRK_PROFILE_*, GRK_EXTENSION_* and (sub)levels values."]
    pub rsiz: u16,
    pub framerate: u16,
    pub write_capture_resolution_from_file: bool,
    pub capture_resolution_from_file: [f64; 2usize],
    pub write_capture_resolution: bool,
    pub capture_resolution: [f64; 2usize],
    pub write_display_resolution: bool,
    pub display_resolution: [f64; 2usize],
    pub apply_icc: bool,
    pub rate_control_algorithm: GRK_RATE_CONTROL_ALGORITHM,
    pub num_threads: u32,
    pub device_id: i32,
    pub duration: u32,
    pub kernel_build_options: u32,
    pub repeats: u32,
    pub write_plt: bool,
    pub write_tlm: bool,
    pub shared_memory_interface: bool,
    pub jpx_branding: bool,
    pub write_rreq: bool,
    pub rreq_standard_features: [u16; 8usize],
    pub num_rreq_standard_features: u8,
    pub geoboxes_after_jp2c: bool,
    #[doc = " Enable progressive rate control during T1 encoding.\n\n When true, the encoder estimates the PCRD slope threshold progressively\n as code blocks complete, and terminates encoding of subsequent blocks early\n when their coding passes are predicted to be discarded by rate control.\n\n This provides significant speedup (20-40% for DCI/high-ratio compression)\n at the cost of non-bit-exact output compared to standard PCRD. Quality\n differences are negligible (within rounding error of the target rate).\n\n Default: false (standard full-encode + PCRD, bit-exact reproducible output)."]
    pub progressive_rate_control: bool,
    pub transcode: bool,
    pub transcode_src: grk_stream_params,
    pub write_sop: bool,
    pub write_eph: bool,
    pub max_layers_transcode: u16,
    pub max_res_transcode: u8,
    pub transcode_prog_order: GRK_PROG_ORDER,
    #[doc = " Apply Rec.709 RGB → DCI X'Y'Z' colour transform on the input image\n before compression. Uses SIMD-accelerated pipeline:\n   1. Rec.709 OETF⁻¹ (gamma → linear)\n   2. 3×3 matrix (linear RGB → linear CIE XYZ, D65)\n   3. DCI 2.6 gamma (linear → X'Y'Z')\n Only applied to images with ≥3 components."]
    pub apply_xyz_transform: bool,
}
#[doc = " @struct grk_cparameters\n @brief Compression parameters"]
pub type grk_cparameters = _grk_cparameters;
extern "C" {
    #[doc = " @brief Fills a @ref grk_cparameters struct with safe default values.\n\n Defaults applied:\n - No rate constraints (lossless)\n - Tile size: entire image (single tile)\n - Codeblock size: 64×64\n - Precinct size: 2^15×2^15 (effectively no sub-precincts)\n - Number of DWT resolutions: 6\n - 1 quality layer\n - No SOP / EPH markers\n - No ROI upshift\n - No mode switches (no LAZY, RESET, etc.)\n - Progression order: LRCP\n - Reversible 5-3 DWT\n - Multi-component transform enabled (when 3+ components present)\n - Image origin (0,0); tile origin (0,0); no POC\n - Sub-sampling: dx=1, dy=1\n\n @param parameters compression parameter block to initialise\n                   (see @ref grk_cparameters); must not be NULL"]
    pub fn grk_compress_set_default_params(parameters: *mut grk_cparameters);
}
extern "C" {
    #[doc = " @brief Apply Rec.709 RGB → DCI X'Y'Z' colour transform to an image in-place.\n\n Operates on planar int32 component buffers (comp[0..2].data).\n Pixel values are interpreted as unsigned integers in [0, (1<<prec)-1].\n\n @param image  Image with ≥3 components. Only components 0,1,2 are modified.\n @return true on success, false if image has <3 components."]
    pub fn grk_apply_xyz_transform(image: *mut grk_image) -> bool;
}
extern "C" {
    #[doc = " @brief Creates and initializes a JPEG 2000 compressor.\n\n Allocates and prepares all compression state for the provided image.\n The output destination (file, buffer, or callbacks) is described by\n @p stream_params. Encoding parameters (tile size, DWT levels, quality\n layers, etc.) are taken from @p parameters — call\n grk_compress_set_default_params() first to populate defaults.\n\n The @p image must remain valid until grk_compress() completes. The\n returned codec object must be released with grk_object_unref().\n\n @param stream_params  output stream description (see @ref grk_stream_params)\n @param parameters     compression settings (see @ref grk_cparameters)\n @param image          source image to compress (see @ref grk_image)\n @return pointer to an opaque @ref grk_object on success, NULL on failure\n         (invalid parameters, unsupported image, OOM)"]
    pub fn grk_compress_init(
        stream_params: *mut grk_stream_params,
        parameters: *mut grk_cparameters,
        image: *mut grk_image,
    ) -> *mut grk_object;
}
extern "C" {
    #[doc = " @brief Compresses the image into a JPEG 2000 codestream.\n\n Performs the full encode pipeline (MCT, DWT, T1, rate control, T2)\n and writes the result to the output stream supplied to grk_compress_init().\n\n The @p tile parameter is only used when a hardware-accelerator plugin\n performs some encoding stages; pass NULL for CPU-only encoding.\n\n @param codec  compression codec (see @ref grk_object)\n @param tile   plugin tile data, or NULL for CPU-only encoding\n               (see @ref grk_plugin_tile)\n @return number of bytes written to the output stream, or 0 on failure"]
    pub fn grk_compress(codec: *mut grk_object, tile: *mut grk_plugin_tile) -> u64;
}
extern "C" {
    #[doc = " @brief Compresses an additional frame into a multi-frame container (MJ2).\n For single-image formats (JP2, J2K) this returns 0 (unsupported).\n @param codec compression codec (see @ref grk_object)\n @param image Input image for this frame (see @ref grk_image)\n @param tile\tplugin tile (see @ref grk_plugin_tile)\n @return number of bytes written if successful, 0 otherwise"]
    pub fn grk_compress_frame(
        codec: *mut grk_object,
        image: *mut grk_image,
        tile: *mut grk_plugin_tile,
    ) -> u64;
}
extern "C" {
    #[doc = " @brief Finalizes a multi-frame compress container (e.g. writes MJ2 moov box).\n For single-image formats this is a no-op.\n @param codec compression codec (see @ref grk_object)\n @return true if successful, otherwise false"]
    pub fn grk_compress_finish(codec: *mut grk_object) -> bool;
}
extern "C" {
    #[doc = " @brief Gets the total compressed length (bytes written to output stream/buffer)\n @param\tcodec\tcompression codec (see @ref grk_object)\n @return\tcompressed length in bytes, or 0 on failure"]
    pub fn grk_compress_get_compressed_length(codec: *mut grk_object) -> u64;
}
extern "C" {
    #[doc = " @brief Transcodes a JPEG 2000 file by rewriting JP2 boxes while copying\n        the codestream verbatim.\n\n Opens the source stream, parses box headers to locate the contiguous\n codestream (JP2C) box, then writes a new JP2 file to @p dst_stream\n using the metadata from @p image and the box-level settings in\n @p parameters (branding, resolution, geoboxes placement, etc.).\n The raw J2K codestream is copied byte-for-byte from source to\n destination without re-encoding.\n\n The @p image must carry the desired output metadata in its\n @c grk_image_meta (XMP, IPTC, EXIF, GeoTIFF, IPR, XML, association\n boxes, channel definitions, colour space, ICC profile, etc.).\n Populate these fields before calling this function — typically by\n decompressing the source header with grk_decompress_read_header(),\n then modifying the metadata as needed.\n\n @param src_stream  source stream description (must be readable,\n                    see @ref grk_stream_params)\n @param dst_stream  destination stream description (must be writable,\n                    see @ref grk_stream_params)\n @param parameters  compression / transcode settings\n                    (see @ref grk_cparameters); box-level fields\n                    (jpx_branding, write_rreq, geoboxes_after_jp2c,\n                    resolution, etc.) are honoured\n @param image       source image carrying component info and metadata\n                    (see @ref grk_image)\n @return number of bytes written to the destination stream, or 0 on failure"]
    pub fn grk_transcode(
        src_stream: *mut grk_stream_params,
        dst_stream: *mut grk_stream_params,
        parameters: *mut grk_cparameters,
        image: *mut grk_image,
    ) -> u64;
}
extern "C" {
    #[doc = " @brief Dumps codec diagnostic information to a stream.\n\n Writes human-readable codec state (image dimensions, tile grid,\n progression order, codestream indices, etc.) to @p output_stream.\n Useful for debugging and conformance testing.\n\n The @p info_flag controls which sections are emitted; combine the\n GRK_IMG_INFO, GRK_MH_INFO, GRK_TH_INFO, GRK_TCH_INFO, GRK_MH_IND,\n and GRK_TH_IND flags as needed.\n\n @param codec          codec (decompression or compression — see @ref grk_object)\n @param info_flag      bitmask of GRK_*_INFO / GRK_*_IND flags\n @param output_stream  destination FILE* (e.g. stdout, stderr, or a log file)"]
    pub fn grk_dump_codec(codec: *mut grk_object, info_flag: u32, output_stream: *mut FILE);
}
extern "C" {
    #[doc = " @brief Installs a custom Multi-Component Transform (MCT) matrix.\n\n Overrides the default RGB→YCbCr colour transform with a user-supplied\n floating-point matrix.  The matrix must be square (@p nb_comp × @p nb_comp)\n stored in row-major order.  A matching DC shift vector (one value per\n component) is applied after the transform.\n\n Only meaningful for compression; ignored for decompression.\n Must be called before grk_compress_init().\n\n @param parameters       compression parameters to update (see @ref grk_cparameters)\n @param encoding_matrix  row-major nb_comp×nb_comp transform matrix\n @param dc_shift         per-component DC shift values (array of @p nb_comp int32_t)\n @param nb_comp          number of image components (matrix dimension)\n @return true if the matrix was stored successfully, false on invalid input"]
    pub fn grk_set_MCT(
        parameters: *mut grk_cparameters,
        encoding_matrix: *const f32,
        dc_shift: *const i32,
        nb_comp: u32,
    ) -> bool;
}
#[doc = " @struct grk_plugin_load_info\n @brief Plugin load info\n"]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct _grk_plugin_load_info {
    pub pluginPath: *const ::std::os::raw::c_char,
}
#[doc = " @struct grk_plugin_load_info\n @brief Plugin load info\n"]
pub type grk_plugin_load_info = _grk_plugin_load_info;
extern "C" {
    #[doc = " @brief Loads a hardware-accelerator plugin (.so / .dll).\n\n Should be called after grk_initialize(), before grk_compress_init() or\n grk_decompress_init().  The plugin extends Grok with GPU or FPGA\n acceleration for T1 entropy coding.  If loading fails, Grok falls back\n to CPU-only operation transparently.\n\n Only one plugin may be loaded at a time.  Call grk_plugin_cleanup() to\n unload the current plugin before loading a new one.\n\n @param info  plugin path and options (see @ref grk_plugin_load_info)\n @return true if the plugin was loaded and its ABI version is compatible,\n         false otherwise"]
    pub fn grk_plugin_load(info: grk_plugin_load_info) -> bool;
}
extern "C" {
    #[doc = " @brief Unloads the plugin and releases all plugin-owned resources.\n\n Signals the plugin to drain any in-flight GPU/FPGA work, then closes\n the shared library handle.  Call before process exit or before loading\n a different plugin version.\n\n Safe to call even if no plugin is loaded (no-op in that case)."]
    pub fn grk_plugin_cleanup();
}
extern "C" {
    #[doc = " @brief Returns the current debug state bitmask of the loaded plugin.\n\n The returned value is a combination of GRK_PLUGIN_STATE_* flags:\n - GRK_PLUGIN_STATE_NO_DEBUG (0x0)         — production mode, no debug output\n - GRK_PLUGIN_STATE_DEBUG (0x1)            — T1 debug comparisons enabled\n - GRK_PLUGIN_STATE_PRE_TR1 (0x2)          — pre-T1 DWT/MCT data compared\n - GRK_PLUGIN_STATE_DWT_QUANTIZATION (0x4) — DWT quantisation compared\n - GRK_PLUGIN_STATE_MCT_ONLY (0x8)         — only MCT stage compared\n\n Returns 0 (GRK_PLUGIN_STATE_NO_DEBUG) if no plugin is loaded.\n\n @return bitmask of active GRK_PLUGIN_STATE_* flags"]
    pub fn grk_plugin_get_debug_state() -> u32;
}
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct _grk_plugin_init_info {
    pub device_id: i32,
    pub verbose: bool,
    pub license: *const ::std::os::raw::c_char,
    pub server: *const ::std::os::raw::c_char,
}
pub type grk_plugin_init_info = _grk_plugin_init_info;
extern "C" {
    #[doc = " @brief Initializes a loaded plugin with a device and license.\n\n Must be called after grk_plugin_load() and before any compress or\n decompress operation that uses the plugin.  Connects the plugin to\n the specified accelerator device and validates the license string.\n\n @param init_info  device ID, license key, and optional server address\n                   (see @ref grk_plugin_init_info)\n @return true if the plugin accepted the device and license,\n         false on invalid license, device unavailable, or no plugin loaded"]
    pub fn grk_plugin_init(init_info: grk_plugin_init_info) -> bool;
}
#[doc = " @struct grk_plugin_compress_user_callback_info\n @brief Plugin compress user callback info"]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct grk_plugin_compress_user_callback_info {
    pub input_file_name: *const ::std::os::raw::c_char,
    pub output_file_name_is_relative: bool,
    pub output_file_name: *const ::std::os::raw::c_char,
    pub compressor_parameters: *mut grk_cparameters,
    pub image: *mut grk_image,
    pub tile: *mut grk_plugin_tile,
    pub stream_params: grk_stream_params,
    pub error_code: ::std::os::raw::c_uint,
}
#[doc = " @brief Plugin compress user callback\n @param info callback info (see @ref grk_plugin_compress_user_callback_info)"]
pub type GRK_PLUGIN_COMPRESS_USER_CALLBACK = ::std::option::Option<
    unsafe extern "C" fn(info: *mut grk_plugin_compress_user_callback_info) -> u64,
>;
#[doc = " @brief Plugin batch compress info"]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct grk_plugin_compress_batch_info {
    pub input_dir: *const ::std::os::raw::c_char,
    pub output_dir: *const ::std::os::raw::c_char,
    pub compress_parameters: *mut grk_cparameters,
    pub callback: GRK_PLUGIN_COMPRESS_USER_CALLBACK,
}
extern "C" {
    #[doc = " @brief Compresses a single image using the loaded hardware plugin.\n\n Uses the plugin-accelerated T1 encoder.  For CPU-only encoding use\n grk_compress() instead.  The @p callback is invoked on completion\n with the compressed byte count and any error code.\n\n @param compress_parameters  compression settings (see @ref grk_cparameters)\n @param callback             completion callback\n                             (see @ref GRK_PLUGIN_COMPRESS_USER_CALLBACK)\n @return 0 on success, non-zero error code on failure"]
    pub fn grk_plugin_compress(
        compress_parameters: *mut grk_cparameters,
        callback: GRK_PLUGIN_COMPRESS_USER_CALLBACK,
    ) -> i32;
}
extern "C" {
    #[doc = " @brief Compresses a directory of images in batch using the hardware plugin.\n\n Scans @p info.input_dir for supported source images, compresses each one\n with plugin acceleration, and writes results to @p info.output_dir.\n A per-image @p info.callback is invoked for each file to allow\n custom output naming and post-processing.\n\n @param info  batch job description: input/output directories, compression\n              parameters, and per-file callback\n              (see @ref grk_plugin_compress_batch_info)\n @return 0 if all files were queued successfully, non-zero on error"]
    pub fn grk_plugin_batch_compress(info: grk_plugin_compress_batch_info) -> i32;
}
extern "C" {
    #[doc = " @brief Blocks until all pending plugin batch jobs have completed.\n\n Should be called after grk_plugin_batch_compress() returns to ensure\n all asynchronously queued compression tasks have finished before the\n caller inspects output files or calls grk_plugin_cleanup()."]
    pub fn grk_plugin_wait_for_batch_complete();
}
extern "C" {
    #[doc = " @brief Requests cancellation of a running batch compress operation.\n\n Sets a stop flag that causes the batch scheduler to skip remaining\n queued files.  Already-in-flight jobs are allowed to complete.\n Call grk_plugin_wait_for_batch_complete() after this to ensure\n a clean shutdown."]
    pub fn grk_plugin_stop_batch_compress();
}
#[doc = " @brief Plugin init decompressors"]
pub type GROK_INIT_DECOMPRESSORS = ::std::option::Option<
    unsafe extern "C" fn(
        header_info: *mut grk_header_info,
        image: *mut grk_image,
    ) -> ::std::os::raw::c_int,
>;
#[doc = " @struct grk_plugin_decompress_callback_info\n @brief Plugin decompress callback info"]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct _grk_plugin_decompress_callback_info {
    pub device_id: usize,
    pub init_decompressors_func: GROK_INIT_DECOMPRESSORS,
    pub input_file_name: *const ::std::os::raw::c_char,
    pub output_file_name: *const ::std::os::raw::c_char,
    pub decod_format: GRK_CODEC_FORMAT,
    pub cod_format: GRK_SUPPORTED_FILE_FMT,
    pub codec: *mut grk_object,
    pub header_info: grk_header_info,
    pub decompressor_parameters: *mut grk_decompress_parameters,
    pub image: *mut grk_image,
    pub plugin_owns_image: bool,
    pub tile: *mut grk_plugin_tile,
    pub error_code: ::std::os::raw::c_uint,
    pub decompress_flags: u32,
    pub full_image_x0: u32,
    pub full_image_y0: u32,
    pub user_data: *mut ::std::os::raw::c_void,
    pub format_private: *mut ::std::os::raw::c_void,
}
#[doc = " @struct grk_plugin_decompress_callback_info\n @brief Plugin decompress callback info"]
pub type grk_plugin_decompress_callback_info = _grk_plugin_decompress_callback_info;
#[doc = " @brief Plugin decompress callback\n @param info callback info (see @ref grk_plugin_decompress_callback_info)\n @return 0 if successful, otherwise return error code"]
pub type grk_plugin_decompress_callback = ::std::option::Option<
    unsafe extern "C" fn(info: *mut grk_plugin_decompress_callback_info) -> i32,
>;
extern "C" {
    #[doc = " @brief Decompresses a single JPEG 2000 image using the loaded hardware plugin.\n\n Uses plugin-accelerated T1 entropy decoding.  For CPU-only decoding use\n grk_decompress() instead.  The @p callback is invoked on completion.\n\n @param decompress_parameters  decompression settings\n                               (see @ref grk_decompress_parameters)\n @param callback               per-image completion callback\n                               (see @ref grk_plugin_decompress_callback)\n @return 0 on success, non-zero error code on failure"]
    pub fn grk_plugin_decompress(
        decompress_parameters: *mut grk_decompress_parameters,
        callback: grk_plugin_decompress_callback,
    ) -> i32;
}
extern "C" {
    #[doc = " @brief Initialises a batch plugin decompress operation but does not start it.\n\n Sets up a batch decompression pipeline: scans @p input_dir, pairs each\n file with @p decompress_parameters, and registers @p callback for\n per-file results.  Call grk_plugin_batch_decompress() to start decoding.\n\n @param input_dir              directory containing JPEG 2000 source files\n @param output_dir             directory to write decompressed output files\n @param decompress_parameters  shared decompression settings for all files\n                               (see @ref grk_decompress_parameters)\n @param callback               per-image result callback\n                               (see @ref grk_plugin_decompress_callback)\n @return 0 on success, non-zero on error (missing directory, bad parameters)"]
    pub fn grk_plugin_init_batch_decompress(
        input_dir: *const ::std::os::raw::c_char,
        output_dir: *const ::std::os::raw::c_char,
        decompress_parameters: *mut grk_decompress_parameters,
        callback: grk_plugin_decompress_callback,
    ) -> i32;
}
extern "C" {
    #[doc = " @brief Starts (resumes) the queued batch decompress operation.\n\n Begins processing files registered by grk_plugin_init_batch_decompress().\n Returns immediately; use grk_plugin_wait_for_batch_complete() (or poll\n via callbacks) to track completion.\n\n @return 0 on success, non-zero if the batch was not initialised or\n         a fatal scheduling error occurred"]
    pub fn grk_plugin_batch_decompress() -> i32;
}
extern "C" {
    #[doc = " @brief Requests cancellation of a running batch decompress operation.\n\n Sets a stop flag that causes the batch scheduler to skip remaining\n files.  Already-in-flight jobs complete normally.  After calling this,\n wait for grk_plugin_wait_for_batch_complete() before accessing output."]
    pub fn grk_plugin_stop_batch_decompress();
}
extern "C" {
    #[doc = " @brief Returns an opaque handle to the core thread-pool executor.\n\n The handle is a pointer to the internal tf::Executor.  Cast it back\n with the helpers in grk_thread_pool.h (C++ only) or use\n grk_num_workers() / grk_worker_id() from C.\n\n @return opaque executor handle, or NULL if the library is not initialised"]
    pub fn grk_thread_pool() -> *mut ::std::os::raw::c_void;
}
extern "C" {
    #[doc = " @brief Returns the number of worker threads in the core thread pool.\n\n @return total number of threads (including the driver thread)"]
    pub fn grk_num_workers() -> usize;
}
extern "C" {
    #[doc = " @brief Returns the TaskFlow worker id of the calling thread.\n\n @return worker id if called from inside a TaskFlow task, 0 otherwise"]
    pub fn grk_worker_id() -> u32;
}
